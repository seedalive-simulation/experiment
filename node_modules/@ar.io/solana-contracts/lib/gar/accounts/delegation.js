/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU128Decoder, getU128Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const DELEGATION_DISCRIMINATOR = new Uint8Array([237, 90, 140, 159, 124, 255, 243, 80]);
export function getDelegationDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(DELEGATION_DISCRIMINATOR); }
/** Gets the encoder for {@link DelegationArgs} account data. */
export function getDelegationEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['gateway', getAddressEncoder()], ['delegator', getAddressEncoder()], ['amount', getU64Encoder()], ['startTimestamp', getI64Encoder()], ['rewardDebt', getU128Encoder()], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: DELEGATION_DISCRIMINATOR }));
}
/** Gets the decoder for {@link Delegation} account data. */
export function getDelegationDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['gateway', getAddressDecoder()], ['delegator', getAddressDecoder()], ['amount', getU64Decoder()], ['startTimestamp', getI64Decoder()], ['rewardDebt', getU128Decoder()], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link Delegation} account data. */
export function getDelegationCodec() {
    return combineCodec(getDelegationEncoder(), getDelegationDecoder());
}
export function decodeDelegation(encodedAccount) {
    return decodeAccount(encodedAccount, getDelegationDecoder());
}
export async function fetchDelegation(rpc, address, config) {
    const maybeAccount = await fetchMaybeDelegation(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeDelegation(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeDelegation(maybeAccount);
}
export async function fetchAllDelegation(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeDelegation(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeDelegation(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeDelegation(maybeAccount));
}
export function getDelegationSize() {
    return 108;
}
//# sourceMappingURL=delegation.js.map