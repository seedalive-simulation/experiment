/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, transformEncoder } from '@solana/kit';
import { getGatewaySlotDecoder, getGatewaySlotEncoder } from '../types/index.js';
export const GATEWAY_REGISTRY_DISCRIMINATOR = new Uint8Array([207, 115, 197, 33, 28, 106, 182, 209]);
export function getGatewayRegistryDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(GATEWAY_REGISTRY_DISCRIMINATOR); }
/** Gets the encoder for {@link GatewayRegistryArgs} account data. */
export function getGatewayRegistryEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['authority', getAddressEncoder()], ['count', getU32Encoder()], ['padding', fixEncoderSize(getBytesEncoder(), 4)], ['gateways', getArrayEncoder(getGatewaySlotEncoder(), { size: 3000 })], ['versionBytes', fixEncoderSize(getBytesEncoder(), 3)], ['padding2', fixEncoderSize(getBytesEncoder(), 5)]]), (value) => ({ ...value, discriminator: GATEWAY_REGISTRY_DISCRIMINATOR }));
}
/** Gets the decoder for {@link GatewayRegistry} account data. */
export function getGatewayRegistryDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['authority', getAddressDecoder()], ['count', getU32Decoder()], ['padding', fixDecoderSize(getBytesDecoder(), 4)], ['gateways', getArrayDecoder(getGatewaySlotDecoder(), { size: 3000 })], ['versionBytes', fixDecoderSize(getBytesDecoder(), 3)], ['padding2', fixDecoderSize(getBytesDecoder(), 5)]]);
}
/** Gets the codec for {@link GatewayRegistry} account data. */
export function getGatewayRegistryCodec() {
    return combineCodec(getGatewayRegistryEncoder(), getGatewayRegistryDecoder());
}
export function decodeGatewayRegistry(encodedAccount) {
    return decodeAccount(encodedAccount, getGatewayRegistryDecoder());
}
export async function fetchGatewayRegistry(rpc, address, config) {
    const maybeAccount = await fetchMaybeGatewayRegistry(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeGatewayRegistry(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeGatewayRegistry(maybeAccount);
}
export async function fetchAllGatewayRegistry(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeGatewayRegistry(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeGatewayRegistry(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeGatewayRegistry(maybeAccount));
}
export function getGatewayRegistrySize() {
    return 168056;
}
//# sourceMappingURL=gatewayRegistry.js.map