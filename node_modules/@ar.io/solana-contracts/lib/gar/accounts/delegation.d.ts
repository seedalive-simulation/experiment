/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { type SchemaVersion, type SchemaVersionArgs } from '../types/index.js';
export declare const DELEGATION_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getDelegationDiscriminatorBytes(): ReadonlyUint8Array;
export type Delegation = {
    discriminator: ReadonlyUint8Array;
    gateway: Address;
    delegator: Address;
    /**
     * Last-settled delegate principal. **Stale-by-design between epochs.**
     *
     * `distribute_epoch` does NOT increment this field per delegate — instead it
     * advances `Gateway.cumulative_reward_per_token`. The live balance is:
     *
     * ```text
     * live = amount + ((gateway.cumulative_reward_per_token - reward_debt) * amount) / REWARD_PRECISION
     * ```
     *
     * Use [`settle_delegate_rewards`] to realize pending rewards into this field
     * (called automatically on every delegation interaction, and exposed as the
     * permissionless `compound_delegation_rewards` instruction).
     *
     * Indexers, wallets, and dashboards must compute the live value from the
     * accumulator; reading `amount` directly under-reports earnings. See
     * `INVARIANTS.md` ("Stale-by-design: delegate rewards").
     */
    amount: bigint;
    startTimestamp: bigint;
    /**
     * Snapshot of `gateway.cumulative_reward_per_token` at the last call to
     * [`settle_delegate_rewards`]. The difference between this and the
     * gateway's current accumulator is what's owed to the delegate.
     */
    rewardDebt: bigint;
    bump: number;
    version: SchemaVersion;
};
export type DelegationArgs = {
    gateway: Address;
    delegator: Address;
    /**
     * Last-settled delegate principal. **Stale-by-design between epochs.**
     *
     * `distribute_epoch` does NOT increment this field per delegate — instead it
     * advances `Gateway.cumulative_reward_per_token`. The live balance is:
     *
     * ```text
     * live = amount + ((gateway.cumulative_reward_per_token - reward_debt) * amount) / REWARD_PRECISION
     * ```
     *
     * Use [`settle_delegate_rewards`] to realize pending rewards into this field
     * (called automatically on every delegation interaction, and exposed as the
     * permissionless `compound_delegation_rewards` instruction).
     *
     * Indexers, wallets, and dashboards must compute the live value from the
     * accumulator; reading `amount` directly under-reports earnings. See
     * `INVARIANTS.md` ("Stale-by-design: delegate rewards").
     */
    amount: number | bigint;
    startTimestamp: number | bigint;
    /**
     * Snapshot of `gateway.cumulative_reward_per_token` at the last call to
     * [`settle_delegate_rewards`]. The difference between this and the
     * gateway's current accumulator is what's owed to the delegate.
     */
    rewardDebt: number | bigint;
    bump: number;
    version: SchemaVersionArgs;
};
/** Gets the encoder for {@link DelegationArgs} account data. */
export declare function getDelegationEncoder(): FixedSizeEncoder<DelegationArgs>;
/** Gets the decoder for {@link Delegation} account data. */
export declare function getDelegationDecoder(): FixedSizeDecoder<Delegation>;
/** Gets the codec for {@link Delegation} account data. */
export declare function getDelegationCodec(): FixedSizeCodec<DelegationArgs, Delegation>;
export declare function decodeDelegation<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Delegation, TAddress>;
export declare function decodeDelegation<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Delegation, TAddress>;
export declare function fetchDelegation<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<Delegation, TAddress>>;
export declare function fetchMaybeDelegation<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<Delegation, TAddress>>;
export declare function fetchAllDelegation(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<Delegation>[]>;
export declare function fetchAllMaybeDelegation(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<Delegation>[]>;
export declare function getDelegationSize(): number;
//# sourceMappingURL=delegation.d.ts.map