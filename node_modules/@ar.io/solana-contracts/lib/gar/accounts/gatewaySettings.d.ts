/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { type SchemaVersion, type SchemaVersionArgs } from '../types/index.js';
export declare const GATEWAY_SETTINGS_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getGatewaySettingsDiscriminatorBytes(): ReadonlyUint8Array;
export type GatewaySettings = {
    discriminator: ReadonlyUint8Array;
    authority: Address;
    mint: Address;
    minOperatorStake: bigint;
    minDelegateStake: bigint;
    withdrawalPeriod: bigint;
    maxExpeditedWithdrawalPenalty: bigint;
    minExpeditedWithdrawalPenalty: bigint;
    minExpeditedWithdrawalAmount: bigint;
    maxDelegatesPerGateway: number;
    /** Whether migration import is active (permanently disabled by finalize_migration) */
    migrationActive: boolean;
    /** Dedicated migration authority (hot key for batch imports) */
    migrationAuthority: Address;
    /** C-1: Pinned stake token account address (validated on every instruction that uses it) */
    stakeTokenAccount: Address;
    /** C-2: Pinned protocol token account address (validated on every instruction that uses it) */
    protocolTokenAccount: Address;
    /**
     * H-4: ArNS program ID. Pinned at init so `prescribe_epoch` can validate
     * the NameRegistry account it reads from cross-program. Replaces the
     * hardcoded string literal previously embedded in `epoch.rs`. Set once
     * via [`crate::initialize`] and migrated for pre-existing accounts via
     * [`crate::migrate_settings_set_arns_program_id`].
     */
    arnsProgramId: Address;
    /**
     * Aggregate supply counters — updated atomically at every staking mutation site.
     * Enables the SDK to return the full AoTokenSupplyData breakdown (parity with Lua).
     *
     * **Invariant (supply-counter shadow):** these counters must always equal
     * the per-entity sums computed across every `Gateway` and `Withdrawal` PDA:
     *
     * ```text
     * total_staked     == Σ Gateway.operator_stake
     * total_delegated  == Σ Gateway.total_delegated_stake
     * total_withdrawn  == Σ Withdrawal.amount
     * ```
     *
     * Any drift indicates a missed-update bug in some instruction. See
     * `INVARIANTS.md` (Invariant 2) for the full health-check formula and the
     * global property test in `programs/ario-gar/tests/integration.rs`.
     */
    totalStaked: bigint;
    totalDelegated: bigint;
    totalWithdrawn: bigint;
    bump: number;
    version: SchemaVersion;
};
export type GatewaySettingsArgs = {
    authority: Address;
    mint: Address;
    minOperatorStake: number | bigint;
    minDelegateStake: number | bigint;
    withdrawalPeriod: number | bigint;
    maxExpeditedWithdrawalPenalty: number | bigint;
    minExpeditedWithdrawalPenalty: number | bigint;
    minExpeditedWithdrawalAmount: number | bigint;
    maxDelegatesPerGateway: number;
    /** Whether migration import is active (permanently disabled by finalize_migration) */
    migrationActive: boolean;
    /** Dedicated migration authority (hot key for batch imports) */
    migrationAuthority: Address;
    /** C-1: Pinned stake token account address (validated on every instruction that uses it) */
    stakeTokenAccount: Address;
    /** C-2: Pinned protocol token account address (validated on every instruction that uses it) */
    protocolTokenAccount: Address;
    /**
     * H-4: ArNS program ID. Pinned at init so `prescribe_epoch` can validate
     * the NameRegistry account it reads from cross-program. Replaces the
     * hardcoded string literal previously embedded in `epoch.rs`. Set once
     * via [`crate::initialize`] and migrated for pre-existing accounts via
     * [`crate::migrate_settings_set_arns_program_id`].
     */
    arnsProgramId: Address;
    /**
     * Aggregate supply counters — updated atomically at every staking mutation site.
     * Enables the SDK to return the full AoTokenSupplyData breakdown (parity with Lua).
     *
     * **Invariant (supply-counter shadow):** these counters must always equal
     * the per-entity sums computed across every `Gateway` and `Withdrawal` PDA:
     *
     * ```text
     * total_staked     == Σ Gateway.operator_stake
     * total_delegated  == Σ Gateway.total_delegated_stake
     * total_withdrawn  == Σ Withdrawal.amount
     * ```
     *
     * Any drift indicates a missed-update bug in some instruction. See
     * `INVARIANTS.md` (Invariant 2) for the full health-check formula and the
     * global property test in `programs/ario-gar/tests/integration.rs`.
     */
    totalStaked: number | bigint;
    totalDelegated: number | bigint;
    totalWithdrawn: number | bigint;
    bump: number;
    version: SchemaVersionArgs;
};
/** Gets the encoder for {@link GatewaySettingsArgs} account data. */
export declare function getGatewaySettingsEncoder(): FixedSizeEncoder<GatewaySettingsArgs>;
/** Gets the decoder for {@link GatewaySettings} account data. */
export declare function getGatewaySettingsDecoder(): FixedSizeDecoder<GatewaySettings>;
/** Gets the codec for {@link GatewaySettings} account data. */
export declare function getGatewaySettingsCodec(): FixedSizeCodec<GatewaySettingsArgs, GatewaySettings>;
export declare function decodeGatewaySettings<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<GatewaySettings, TAddress>;
export declare function decodeGatewaySettings<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<GatewaySettings, TAddress>;
export declare function fetchGatewaySettings<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<GatewaySettings, TAddress>>;
export declare function fetchMaybeGatewaySettings<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<GatewaySettings, TAddress>>;
export declare function fetchAllGatewaySettings(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<GatewaySettings>[]>;
export declare function fetchAllMaybeGatewaySettings(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<GatewaySettings>[]>;
export declare function getGatewaySettingsSize(): number;
//# sourceMappingURL=gatewaySettings.d.ts.map