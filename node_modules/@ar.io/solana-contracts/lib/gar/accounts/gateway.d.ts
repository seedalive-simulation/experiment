/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type Option, type OptionOrNullable, type ReadonlyUint8Array } from '@solana/kit';
import { type GatewaySettings2, type GatewaySettings2Args, type GatewayStats, type GatewayStatsArgs, type GatewayStatus, type GatewayStatusArgs, type GatewayWeights, type GatewayWeightsArgs, type Protocol, type ProtocolArgs, type RegistryIndex, type RegistryIndexArgs, type SchemaVersion, type SchemaVersionArgs } from '../types/index.js';
export declare const GATEWAY_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getGatewayDiscriminatorBytes(): ReadonlyUint8Array;
export type Gateway = {
    discriminator: ReadonlyUint8Array;
    operator: Address;
    label: string;
    fqdn: string;
    port: number;
    protocol: Protocol;
    properties: string;
    note: string;
    operatorStake: bigint;
    totalDelegatedStake: bigint;
    status: GatewayStatus;
    startTimestamp: bigint;
    leaveTimestamp: Option<bigint>;
    /**
     * Snapshot of `epoch_settings.epoch_duration` (seconds) taken when this
     * gateway transitioned to Leaving via `leave_network` or `prune_gateway`.
     * Used by `finalize_gone` to compute the GC eligibility window
     * (`leave_timestamp + GATEWAY_LEAVE_PERIOD + 7 * leave_epoch_duration`)
     * without depending on a possibly-mutated live `epoch_settings.epoch_duration`.
     * Zero for gateways that never left (Joined). Defense-in-depth: `finalize_gone`
     * also takes `max(this snapshot, current_settings.epoch_duration)` so an
     * admin shortening of `epoch_duration` cannot retroactively bring forward
     * a leaver's GC eligibility.
     */
    leaveEpochDuration: bigint;
    stats: GatewayStats;
    weights: GatewayWeights;
    settings: GatewaySettings2;
    registryIndex: RegistryIndex;
    /**
     * M3: Separate observer address (matches Lua: observerAddress)
     * Defaults to operator address if not set. Allows a different wallet to submit observations.
     */
    observerAddress: Address;
    /**
     * Cumulative reward per unit of delegated stake (scaled by REWARD_PRECISION = 1e18)
     * Increases each epoch during distribute_epoch
     */
    cumulativeRewardPerToken: bigint;
    bump: number;
    version: SchemaVersion;
};
export type GatewayArgs = {
    operator: Address;
    label: string;
    fqdn: string;
    port: number;
    protocol: ProtocolArgs;
    properties: string;
    note: string;
    operatorStake: number | bigint;
    totalDelegatedStake: number | bigint;
    status: GatewayStatusArgs;
    startTimestamp: number | bigint;
    leaveTimestamp: OptionOrNullable<number | bigint>;
    /**
     * Snapshot of `epoch_settings.epoch_duration` (seconds) taken when this
     * gateway transitioned to Leaving via `leave_network` or `prune_gateway`.
     * Used by `finalize_gone` to compute the GC eligibility window
     * (`leave_timestamp + GATEWAY_LEAVE_PERIOD + 7 * leave_epoch_duration`)
     * without depending on a possibly-mutated live `epoch_settings.epoch_duration`.
     * Zero for gateways that never left (Joined). Defense-in-depth: `finalize_gone`
     * also takes `max(this snapshot, current_settings.epoch_duration)` so an
     * admin shortening of `epoch_duration` cannot retroactively bring forward
     * a leaver's GC eligibility.
     */
    leaveEpochDuration: number | bigint;
    stats: GatewayStatsArgs;
    weights: GatewayWeightsArgs;
    settings: GatewaySettings2Args;
    registryIndex: RegistryIndexArgs;
    /**
     * M3: Separate observer address (matches Lua: observerAddress)
     * Defaults to operator address if not set. Allows a different wallet to submit observations.
     */
    observerAddress: Address;
    /**
     * Cumulative reward per unit of delegated stake (scaled by REWARD_PRECISION = 1e18)
     * Increases each epoch during distribute_epoch
     */
    cumulativeRewardPerToken: number | bigint;
    bump: number;
    version: SchemaVersionArgs;
};
/** Gets the encoder for {@link GatewayArgs} account data. */
export declare function getGatewayEncoder(): Encoder<GatewayArgs>;
/** Gets the decoder for {@link Gateway} account data. */
export declare function getGatewayDecoder(): Decoder<Gateway>;
/** Gets the codec for {@link Gateway} account data. */
export declare function getGatewayCodec(): Codec<GatewayArgs, Gateway>;
export declare function decodeGateway<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Gateway, TAddress>;
export declare function decodeGateway<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Gateway, TAddress>;
export declare function fetchGateway<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<Gateway, TAddress>>;
export declare function fetchMaybeGateway<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<Gateway, TAddress>>;
export declare function fetchAllGateway(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<Gateway>[]>;
export declare function fetchAllMaybeGateway(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<Gateway>[]>;
//# sourceMappingURL=gateway.d.ts.map