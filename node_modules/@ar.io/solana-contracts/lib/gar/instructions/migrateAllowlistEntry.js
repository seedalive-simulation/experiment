/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findMigrateAllowlistEntryAllowlistEntryPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_ALLOWLIST_ENTRY_DISCRIMINATOR = new Uint8Array([6, 165, 82, 115, 160, 101, 136, 7]);
export function getMigrateAllowlistEntryDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_ALLOWLIST_ENTRY_DISCRIMINATOR); }
export function getMigrateAllowlistEntryInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: MIGRATE_ALLOWLIST_ENTRY_DISCRIMINATOR }));
}
export function getMigrateAllowlistEntryInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getMigrateAllowlistEntryInstructionDataCodec() {
    return combineCodec(getMigrateAllowlistEntryInstructionDataEncoder(), getMigrateAllowlistEntryInstructionDataDecoder());
}
export async function getMigrateAllowlistEntryInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { gateway: { value: input.gateway ?? null, isWritable: false }, delegate: { value: input.delegate ?? null, isWritable: false }, allowlistEntry: { value: input.allowlistEntry ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.allowlistEntry.value) {
        accounts.allowlistEntry.value = await findMigrateAllowlistEntryAllowlistEntryPda({ gateway: getAddressFromResolvedInstructionAccount("gateway", accounts.gateway.value), delegate: getAddressFromResolvedInstructionAccount("delegate", accounts.delegate.value) }, { programAddress });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("gateway", accounts.gateway), getAccountMeta("delegate", accounts.delegate), getAccountMeta("allowlistEntry", accounts.allowlistEntry), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateAllowlistEntryInstructionDataEncoder().encode({}), programAddress });
}
export function getMigrateAllowlistEntryInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { gateway: { value: input.gateway ?? null, isWritable: false }, delegate: { value: input.delegate ?? null, isWritable: false }, allowlistEntry: { value: input.allowlistEntry ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("gateway", accounts.gateway), getAccountMeta("delegate", accounts.delegate), getAccountMeta("allowlistEntry", accounts.allowlistEntry), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateAllowlistEntryInstructionDataEncoder().encode({}), programAddress });
}
export function parseMigrateAllowlistEntryInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { gateway: getNextAccount(), delegate: getNextAccount(), allowlistEntry: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() }, data: getMigrateAllowlistEntryInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migrateAllowlistEntry.js.map