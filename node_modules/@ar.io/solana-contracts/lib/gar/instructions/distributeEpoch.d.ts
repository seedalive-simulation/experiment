/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const DISTRIBUTE_EPOCH_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getDistributeEpochDiscriminatorBytes(): ReadonlyUint8Array;
export type DistributeEpochInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountEpochSettings extends string | AccountMeta<string> = string, TAccountEpoch extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountSettings extends string | AccountMeta<string> = string, TAccountProtocolTokenAccount extends string | AccountMeta<string> = string, TAccountStakeTokenAccount extends string | AccountMeta<string> = string, TAccountArioConfig extends string | AccountMeta<string> = string, TAccountArioCoreProgram extends string | AccountMeta<string> = "ARioCoreProgramXXXXXXXXXXXXXXXXXXXXXXXXXXXX", TAccountPayer extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEpochSettings extends string ? ReadonlyAccount<TAccountEpochSettings> : TAccountEpochSettings, TAccountEpoch extends string ? WritableAccount<TAccountEpoch> : TAccountEpoch, TAccountRegistry extends string ? ReadonlyAccount<TAccountRegistry> : TAccountRegistry, TAccountSettings extends string ? WritableAccount<TAccountSettings> : TAccountSettings, TAccountProtocolTokenAccount extends string ? WritableAccount<TAccountProtocolTokenAccount> : TAccountProtocolTokenAccount, TAccountStakeTokenAccount extends string ? WritableAccount<TAccountStakeTokenAccount> : TAccountStakeTokenAccount, TAccountArioConfig extends string ? ReadonlyAccount<TAccountArioConfig> : TAccountArioConfig, TAccountArioCoreProgram extends string ? ReadonlyAccount<TAccountArioCoreProgram> : TAccountArioCoreProgram, TAccountPayer extends string ? ReadonlySignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, ...TRemainingAccounts]>;
export type DistributeEpochInstructionData = {
    discriminator: ReadonlyUint8Array;
    epochIndex: bigint;
};
export type DistributeEpochInstructionDataArgs = {
    epochIndex: number | bigint;
};
export declare function getDistributeEpochInstructionDataEncoder(): FixedSizeEncoder<DistributeEpochInstructionDataArgs>;
export declare function getDistributeEpochInstructionDataDecoder(): FixedSizeDecoder<DistributeEpochInstructionData>;
export declare function getDistributeEpochInstructionDataCodec(): FixedSizeCodec<DistributeEpochInstructionDataArgs, DistributeEpochInstructionData>;
export type DistributeEpochAsyncInput<TAccountEpochSettings extends string = string, TAccountEpoch extends string = string, TAccountRegistry extends string = string, TAccountSettings extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountStakeTokenAccount extends string = string, TAccountArioConfig extends string = string, TAccountArioCoreProgram extends string = string, TAccountPayer extends string = string, TAccountTokenProgram extends string = string> = {
    epochSettings?: Address<TAccountEpochSettings>;
    epoch?: Address<TAccountEpoch>;
    registry?: Address<TAccountRegistry>;
    /**
     * GAR settings PDA — signs the CPI into `ario_core::release_treasury_to_recipient`
     * (the SPL transfer itself is signed inside ario-core by ArioConfig).
     */
    settings?: Address<TAccountSettings>;
    /**
     * Protocol token account (source of reward tokens). Owned by
     * ario-core's ArioConfig PDA; ario-core signs the SPL transfer.
     * We constrain only the address (matches the settings-pinned value)
     * — the SPL-level owner mismatch with `settings` here is intentional
     * since ario-core now holds treasury authority.
     */
    protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
    /** Stake token account (destination for staked rewards) */
    stakeTokenAccount: Address<TAccountStakeTokenAccount>;
    /**
     * ario-core's `ArioConfig` PDA. ario-core's
     * `release_treasury_to_recipient` ix verifies this is the canonical
     * config PDA (via its own `seeds = [CONFIG_SEED]` constraint).
     * We pass it through as `AccountInfo` here to avoid importing
     * ario-core types (would create a Cargo dep cycle).
     *
     */
    arioConfig?: Address<TAccountArioConfig>;
    /**
     * is patched at build time by `build-sbf.sh --sync-from-manifest`
     * from `program-ids/<cluster>.json`. The constraint guarantees the
     * downstream `invoke_signed` target can't be substituted by the
     * caller.
     */
    arioCoreProgram?: Address<TAccountArioCoreProgram>;
    payer: TransactionSigner<TAccountPayer>;
    tokenProgram?: Address<TAccountTokenProgram>;
    epochIndex: DistributeEpochInstructionDataArgs["epochIndex"];
};
export declare function getDistributeEpochInstructionAsync<TAccountEpochSettings extends string, TAccountEpoch extends string, TAccountRegistry extends string, TAccountSettings extends string, TAccountProtocolTokenAccount extends string, TAccountStakeTokenAccount extends string, TAccountArioConfig extends string, TAccountArioCoreProgram extends string, TAccountPayer extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: DistributeEpochAsyncInput<TAccountEpochSettings, TAccountEpoch, TAccountRegistry, TAccountSettings, TAccountProtocolTokenAccount, TAccountStakeTokenAccount, TAccountArioConfig, TAccountArioCoreProgram, TAccountPayer, TAccountTokenProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<DistributeEpochInstruction<TProgramAddress, TAccountEpochSettings, TAccountEpoch, TAccountRegistry, TAccountSettings, TAccountProtocolTokenAccount, TAccountStakeTokenAccount, TAccountArioConfig, TAccountArioCoreProgram, TAccountPayer, TAccountTokenProgram>>;
export type DistributeEpochInput<TAccountEpochSettings extends string = string, TAccountEpoch extends string = string, TAccountRegistry extends string = string, TAccountSettings extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountStakeTokenAccount extends string = string, TAccountArioConfig extends string = string, TAccountArioCoreProgram extends string = string, TAccountPayer extends string = string, TAccountTokenProgram extends string = string> = {
    epochSettings: Address<TAccountEpochSettings>;
    epoch: Address<TAccountEpoch>;
    registry: Address<TAccountRegistry>;
    /**
     * GAR settings PDA — signs the CPI into `ario_core::release_treasury_to_recipient`
     * (the SPL transfer itself is signed inside ario-core by ArioConfig).
     */
    settings: Address<TAccountSettings>;
    /**
     * Protocol token account (source of reward tokens). Owned by
     * ario-core's ArioConfig PDA; ario-core signs the SPL transfer.
     * We constrain only the address (matches the settings-pinned value)
     * — the SPL-level owner mismatch with `settings` here is intentional
     * since ario-core now holds treasury authority.
     */
    protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
    /** Stake token account (destination for staked rewards) */
    stakeTokenAccount: Address<TAccountStakeTokenAccount>;
    /**
     * ario-core's `ArioConfig` PDA. ario-core's
     * `release_treasury_to_recipient` ix verifies this is the canonical
     * config PDA (via its own `seeds = [CONFIG_SEED]` constraint).
     * We pass it through as `AccountInfo` here to avoid importing
     * ario-core types (would create a Cargo dep cycle).
     *
     */
    arioConfig: Address<TAccountArioConfig>;
    /**
     * is patched at build time by `build-sbf.sh --sync-from-manifest`
     * from `program-ids/<cluster>.json`. The constraint guarantees the
     * downstream `invoke_signed` target can't be substituted by the
     * caller.
     */
    arioCoreProgram?: Address<TAccountArioCoreProgram>;
    payer: TransactionSigner<TAccountPayer>;
    tokenProgram?: Address<TAccountTokenProgram>;
    epochIndex: DistributeEpochInstructionDataArgs["epochIndex"];
};
export declare function getDistributeEpochInstruction<TAccountEpochSettings extends string, TAccountEpoch extends string, TAccountRegistry extends string, TAccountSettings extends string, TAccountProtocolTokenAccount extends string, TAccountStakeTokenAccount extends string, TAccountArioConfig extends string, TAccountArioCoreProgram extends string, TAccountPayer extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: DistributeEpochInput<TAccountEpochSettings, TAccountEpoch, TAccountRegistry, TAccountSettings, TAccountProtocolTokenAccount, TAccountStakeTokenAccount, TAccountArioConfig, TAccountArioCoreProgram, TAccountPayer, TAccountTokenProgram>, config?: {
    programAddress?: TProgramAddress;
}): DistributeEpochInstruction<TProgramAddress, TAccountEpochSettings, TAccountEpoch, TAccountRegistry, TAccountSettings, TAccountProtocolTokenAccount, TAccountStakeTokenAccount, TAccountArioConfig, TAccountArioCoreProgram, TAccountPayer, TAccountTokenProgram>;
export type ParsedDistributeEpochInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        epochSettings: TAccountMetas[0];
        epoch: TAccountMetas[1];
        registry: TAccountMetas[2];
        /**
         * GAR settings PDA — signs the CPI into `ario_core::release_treasury_to_recipient`
         * (the SPL transfer itself is signed inside ario-core by ArioConfig).
         */
        settings: TAccountMetas[3];
        /**
         * Protocol token account (source of reward tokens). Owned by
         * ario-core's ArioConfig PDA; ario-core signs the SPL transfer.
         * We constrain only the address (matches the settings-pinned value)
         * — the SPL-level owner mismatch with `settings` here is intentional
         * since ario-core now holds treasury authority.
         */
        protocolTokenAccount: TAccountMetas[4];
        /** Stake token account (destination for staked rewards) */
        stakeTokenAccount: TAccountMetas[5];
        /**
         * ario-core's `ArioConfig` PDA. ario-core's
         * `release_treasury_to_recipient` ix verifies this is the canonical
         * config PDA (via its own `seeds = [CONFIG_SEED]` constraint).
         * We pass it through as `AccountInfo` here to avoid importing
         * ario-core types (would create a Cargo dep cycle).
         *
         */
        arioConfig: TAccountMetas[6];
        /**
         * is patched at build time by `build-sbf.sh --sync-from-manifest`
         * from `program-ids/<cluster>.json`. The constraint guarantees the
         * downstream `invoke_signed` target can't be substituted by the
         * caller.
         */
        arioCoreProgram: TAccountMetas[7];
        payer: TAccountMetas[8];
        tokenProgram: TAccountMetas[9];
    };
    data: DistributeEpochInstructionData;
};
export declare function parseDistributeEpochInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedDistributeEpochInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=distributeEpoch.d.ts.map