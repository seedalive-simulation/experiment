/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findEpochSettingsPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const CLOSE_EPOCH_SETTINGS_DISCRIMINATOR = new Uint8Array([240, 89, 172, 197, 149, 66, 45, 162]);
export function getCloseEpochSettingsDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_EPOCH_SETTINGS_DISCRIMINATOR); }
export function getCloseEpochSettingsInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_EPOCH_SETTINGS_DISCRIMINATOR }));
}
export function getCloseEpochSettingsInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getCloseEpochSettingsInstructionDataCodec() {
    return combineCodec(getCloseEpochSettingsInstructionDataEncoder(), getCloseEpochSettingsInstructionDataDecoder());
}
export async function getCloseEpochSettingsInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.epochSettings.value) {
        accounts.epochSettings.value = await findEpochSettingsPda();
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("authority", accounts.authority)], data: getCloseEpochSettingsInstructionDataEncoder().encode({}), programAddress });
}
export function getCloseEpochSettingsInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("authority", accounts.authority)], data: getCloseEpochSettingsInstructionDataEncoder().encode({}), programAddress });
}
export function parseCloseEpochSettingsInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { epochSettings: getNextAccount(), authority: getNextAccount() }, data: getCloseEpochSettingsInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=closeEpochSettings.js.map