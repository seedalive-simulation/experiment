/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getNonNullResolvedInstructionInput } from '@solana/program-client-core';
import { findEpochPda, findEpochSettingsPda, findRegistryPda, findSettingsPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const DISTRIBUTE_EPOCH_DISCRIMINATOR = new Uint8Array([240, 137, 247, 4, 136, 95, 63, 68]);
export function getDistributeEpochDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(DISTRIBUTE_EPOCH_DISCRIMINATOR); }
export function getDistributeEpochInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['epochIndex', getU64Encoder()]]), (value) => ({ ...value, discriminator: DISTRIBUTE_EPOCH_DISCRIMINATOR }));
}
export function getDistributeEpochInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['epochIndex', getU64Decoder()]]);
}
export function getDistributeEpochInstructionDataCodec() {
    return combineCodec(getDistributeEpochInstructionDataEncoder(), getDistributeEpochInstructionDataDecoder());
}
export async function getDistributeEpochInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: false }, epoch: { value: input.epoch ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: false }, settings: { value: input.settings ?? null, isWritable: true }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, stakeTokenAccount: { value: input.stakeTokenAccount ?? null, isWritable: true }, arioConfig: { value: input.arioConfig ?? null, isWritable: false }, arioCoreProgram: { value: input.arioCoreProgram ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.epochSettings.value) {
        accounts.epochSettings.value = await findEpochSettingsPda();
    }
    if (!accounts.epoch.value) {
        accounts.epoch.value = await findEpochPda({ epochIndex: getNonNullResolvedInstructionInput("epochIndex", args.epochIndex) }, { programAddress });
    }
    if (!accounts.registry.value) {
        accounts.registry.value = await findRegistryPda();
    }
    if (!accounts.settings.value) {
        accounts.settings.value = await findSettingsPda();
    }
    if (!accounts.arioConfig.value) {
        accounts.arioConfig.value = await getProgramDerivedAddress({ programAddress: 'ARioCoreProgramXXXXXXXXXXXXXXXXXXXXXXXXXXXX', seeds: [getBytesEncoder().encode(new Uint8Array([97, 114, 105, 111, 95, 99, 111, 110, 102, 105, 103]))] });
    }
    if (!accounts.arioCoreProgram.value) {
        accounts.arioCoreProgram.value = 'ARioCoreProgramXXXXXXXXXXXXXXXXXXXXXXXXXXXX';
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("epoch", accounts.epoch), getAccountMeta("registry", accounts.registry), getAccountMeta("settings", accounts.settings), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("stakeTokenAccount", accounts.stakeTokenAccount), getAccountMeta("arioConfig", accounts.arioConfig), getAccountMeta("arioCoreProgram", accounts.arioCoreProgram), getAccountMeta("payer", accounts.payer), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getDistributeEpochInstructionDataEncoder().encode(args), programAddress });
}
export function getDistributeEpochInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: false }, epoch: { value: input.epoch ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: false }, settings: { value: input.settings ?? null, isWritable: true }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, stakeTokenAccount: { value: input.stakeTokenAccount ?? null, isWritable: true }, arioConfig: { value: input.arioConfig ?? null, isWritable: false }, arioCoreProgram: { value: input.arioCoreProgram ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.arioCoreProgram.value) {
        accounts.arioCoreProgram.value = 'ARioCoreProgramXXXXXXXXXXXXXXXXXXXXXXXXXXXX';
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("epoch", accounts.epoch), getAccountMeta("registry", accounts.registry), getAccountMeta("settings", accounts.settings), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("stakeTokenAccount", accounts.stakeTokenAccount), getAccountMeta("arioConfig", accounts.arioConfig), getAccountMeta("arioCoreProgram", accounts.arioCoreProgram), getAccountMeta("payer", accounts.payer), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getDistributeEpochInstructionDataEncoder().encode(args), programAddress });
}
export function parseDistributeEpochInstruction(instruction) {
    if (instruction.accounts.length < 10) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 10 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { epochSettings: getNextAccount(), epoch: getNextAccount(), registry: getNextAccount(), settings: getNextAccount(), protocolTokenAccount: getNextAccount(), stakeTokenAccount: getNextAccount(), arioConfig: getNextAccount(), arioCoreProgram: getNextAccount(), payer: getNextAccount(), tokenProgram: getNextAccount() }, data: getDistributeEpochInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=distributeEpoch.js.map