/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, getNonNullResolvedInstructionInput } from '@solana/program-client-core';
import { findObservationPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_OBSERVATION_DISCRIMINATOR = new Uint8Array([87, 20, 145, 213, 237, 148, 189, 8]);
export function getMigrateObservationDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_OBSERVATION_DISCRIMINATOR); }
export function getMigrateObservationInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['epochIndex', getU64Encoder()]]), (value) => ({ ...value, discriminator: MIGRATE_OBSERVATION_DISCRIMINATOR }));
}
export function getMigrateObservationInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['epochIndex', getU64Decoder()]]);
}
export function getMigrateObservationInstructionDataCodec() {
    return combineCodec(getMigrateObservationInstructionDataEncoder(), getMigrateObservationInstructionDataDecoder());
}
export async function getMigrateObservationInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { observer: { value: input.observer ?? null, isWritable: false }, observation: { value: input.observation ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.observation.value) {
        accounts.observation.value = await findObservationPda({ epochIndex: getNonNullResolvedInstructionInput("epochIndex", args.epochIndex), observer: getAddressFromResolvedInstructionAccount("observer", accounts.observer.value) }, { programAddress });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("observer", accounts.observer), getAccountMeta("observation", accounts.observation), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateObservationInstructionDataEncoder().encode(args), programAddress });
}
export function getMigrateObservationInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { observer: { value: input.observer ?? null, isWritable: false }, observation: { value: input.observation ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("observer", accounts.observer), getAccountMeta("observation", accounts.observation), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateObservationInstructionDataEncoder().encode(args), programAddress });
}
export function parseMigrateObservationInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { observer: getNextAccount(), observation: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() }, data: getMigrateObservationInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migrateObservation.js.map