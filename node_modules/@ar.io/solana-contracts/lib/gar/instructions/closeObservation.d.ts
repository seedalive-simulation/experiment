/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const CLOSE_OBSERVATION_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getCloseObservationDiscriminatorBytes(): ReadonlyUint8Array;
export type CloseObservationInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountEpoch extends string | AccountMeta<string> = string, TAccountObservation extends string | AccountMeta<string> = string, TAccountObserver extends string | AccountMeta<string> = string, TAccountCaller extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEpoch extends string ? WritableAccount<TAccountEpoch> : TAccountEpoch, TAccountObservation extends string ? WritableAccount<TAccountObservation> : TAccountObservation, TAccountObserver extends string ? WritableAccount<TAccountObserver> : TAccountObserver, TAccountCaller extends string ? WritableSignerAccount<TAccountCaller> & AccountSignerMeta<TAccountCaller> : TAccountCaller, ...TRemainingAccounts]>;
export type CloseObservationInstructionData = {
    discriminator: ReadonlyUint8Array;
    epochIndex: bigint;
};
export type CloseObservationInstructionDataArgs = {
    epochIndex: number | bigint;
};
export declare function getCloseObservationInstructionDataEncoder(): FixedSizeEncoder<CloseObservationInstructionDataArgs>;
export declare function getCloseObservationInstructionDataDecoder(): FixedSizeDecoder<CloseObservationInstructionData>;
export declare function getCloseObservationInstructionDataCodec(): FixedSizeCodec<CloseObservationInstructionDataArgs, CloseObservationInstructionData>;
export type CloseObservationAsyncInput<TAccountEpoch extends string = string, TAccountObservation extends string = string, TAccountObserver extends string = string, TAccountCaller extends string = string> = {
    epoch?: Address<TAccountEpoch>;
    observation: Address<TAccountObservation>;
    /**
     * `address = observation.observer` constraint below — that constraint IS
     * the security check (the account must be exactly the recorded observer).
     * It is intentionally an `UncheckedAccount`, NOT a `SystemAccount`: a
     * gateway's `observer_address` may be a PDA / multisig (Squads) /
     * smart-contract wallet (program-owned) that still signs
     * `save_observations` via CPI. A `SystemAccount` would enforce
     * System-Program ownership and fail `close_observation` with
     * `AccountOwnedByWrongProgram` for such observers, leaving the
     * Observation PDA permanently unclosable — and since `close_epoch`
     * requires every observation closed, that epoch could never close
     * (stranded rent/state). The `close = observer` constraint redirects the
     * reclaimed rent here, so a third-party `caller` cannot pocket it. Does
     * not sign — it only receives lamports.
     */
    observer: Address<TAccountObserver>;
    /**
     * Permissionless caller — anyone willing to pay the tx fee can close a
     * distributed epoch's observation. Rent always goes to `observer`
     * regardless of who signs.
     */
    caller: TransactionSigner<TAccountCaller>;
    epochIndex: CloseObservationInstructionDataArgs["epochIndex"];
};
export declare function getCloseObservationInstructionAsync<TAccountEpoch extends string, TAccountObservation extends string, TAccountObserver extends string, TAccountCaller extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: CloseObservationAsyncInput<TAccountEpoch, TAccountObservation, TAccountObserver, TAccountCaller>, config?: {
    programAddress?: TProgramAddress;
}): Promise<CloseObservationInstruction<TProgramAddress, TAccountEpoch, TAccountObservation, TAccountObserver, TAccountCaller>>;
export type CloseObservationInput<TAccountEpoch extends string = string, TAccountObservation extends string = string, TAccountObserver extends string = string, TAccountCaller extends string = string> = {
    epoch: Address<TAccountEpoch>;
    observation: Address<TAccountObservation>;
    /**
     * `address = observation.observer` constraint below — that constraint IS
     * the security check (the account must be exactly the recorded observer).
     * It is intentionally an `UncheckedAccount`, NOT a `SystemAccount`: a
     * gateway's `observer_address` may be a PDA / multisig (Squads) /
     * smart-contract wallet (program-owned) that still signs
     * `save_observations` via CPI. A `SystemAccount` would enforce
     * System-Program ownership and fail `close_observation` with
     * `AccountOwnedByWrongProgram` for such observers, leaving the
     * Observation PDA permanently unclosable — and since `close_epoch`
     * requires every observation closed, that epoch could never close
     * (stranded rent/state). The `close = observer` constraint redirects the
     * reclaimed rent here, so a third-party `caller` cannot pocket it. Does
     * not sign — it only receives lamports.
     */
    observer: Address<TAccountObserver>;
    /**
     * Permissionless caller — anyone willing to pay the tx fee can close a
     * distributed epoch's observation. Rent always goes to `observer`
     * regardless of who signs.
     */
    caller: TransactionSigner<TAccountCaller>;
    epochIndex: CloseObservationInstructionDataArgs["epochIndex"];
};
export declare function getCloseObservationInstruction<TAccountEpoch extends string, TAccountObservation extends string, TAccountObserver extends string, TAccountCaller extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: CloseObservationInput<TAccountEpoch, TAccountObservation, TAccountObserver, TAccountCaller>, config?: {
    programAddress?: TProgramAddress;
}): CloseObservationInstruction<TProgramAddress, TAccountEpoch, TAccountObservation, TAccountObserver, TAccountCaller>;
export type ParsedCloseObservationInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        epoch: TAccountMetas[0];
        observation: TAccountMetas[1];
        /**
         * `address = observation.observer` constraint below — that constraint IS
         * the security check (the account must be exactly the recorded observer).
         * It is intentionally an `UncheckedAccount`, NOT a `SystemAccount`: a
         * gateway's `observer_address` may be a PDA / multisig (Squads) /
         * smart-contract wallet (program-owned) that still signs
         * `save_observations` via CPI. A `SystemAccount` would enforce
         * System-Program ownership and fail `close_observation` with
         * `AccountOwnedByWrongProgram` for such observers, leaving the
         * Observation PDA permanently unclosable — and since `close_epoch`
         * requires every observation closed, that epoch could never close
         * (stranded rent/state). The `close = observer` constraint redirects the
         * reclaimed rent here, so a third-party `caller` cannot pocket it. Does
         * not sign — it only receives lamports.
         */
        observer: TAccountMetas[2];
        /**
         * Permissionless caller — anyone willing to pay the tx fee can close a
         * distributed epoch's observation. Rent always goes to `observer`
         * regardless of who signs.
         */
        caller: TAccountMetas[3];
    };
    data: CloseObservationInstructionData;
};
export declare function parseCloseObservationInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCloseObservationInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=closeObservation.d.ts.map