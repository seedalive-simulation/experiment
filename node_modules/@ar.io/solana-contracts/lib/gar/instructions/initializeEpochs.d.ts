/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const INITIALIZE_EPOCHS_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getInitializeEpochsDiscriminatorBytes(): ReadonlyUint8Array;
export type InitializeEpochsInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountEpochSettings extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountProgramData extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEpochSettings extends string ? WritableAccount<TAccountEpochSettings> : TAccountEpochSettings, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountProgramData extends string ? ReadonlyAccount<TAccountProgramData> : TAccountProgramData, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type InitializeEpochsInstructionData = {
    discriminator: ReadonlyUint8Array;
    authority: Address;
    epochDuration: bigint;
    observerCount: number;
    nameCount: number;
    minObserverStake: bigint;
    slashRate: number;
    /**
     * Denominator (seconds) for the per-gateway tenure-weight ramp:
     * `tenure_weight = min(time_running / tenure_weight_duration,
     * max_tenure_weight)`. Lua / mainnet value is `180 * 86_400`
     * (180 days). Devnet bootstraps often pass `3600` (1 hour) to
     * surface a full tenure ramp inside a handful of short epochs.
     * MUST be > 0 — see ADR / DECISIONS for the security history
     * behind making this a caller-supplied param (the previous
     * unconditional 1-hour default leaked into a production-bound
     * build and inflated weighted-observer-selection probabilities
     * for newly-joined gateways).
     */
    tenureWeightDuration: bigint;
    /**
     * Ceiling for the tenure-weight ramp above. Lua / mainnet value
     * is `4`. MUST be > 0.
     */
    maxTenureWeight: bigint;
};
export type InitializeEpochsInstructionDataArgs = {
    authority: Address;
    epochDuration: number | bigint;
    observerCount: number;
    nameCount: number;
    minObserverStake: number | bigint;
    slashRate: number;
    /**
     * Denominator (seconds) for the per-gateway tenure-weight ramp:
     * `tenure_weight = min(time_running / tenure_weight_duration,
     * max_tenure_weight)`. Lua / mainnet value is `180 * 86_400`
     * (180 days). Devnet bootstraps often pass `3600` (1 hour) to
     * surface a full tenure ramp inside a handful of short epochs.
     * MUST be > 0 — see ADR / DECISIONS for the security history
     * behind making this a caller-supplied param (the previous
     * unconditional 1-hour default leaked into a production-bound
     * build and inflated weighted-observer-selection probabilities
     * for newly-joined gateways).
     */
    tenureWeightDuration: number | bigint;
    /**
     * Ceiling for the tenure-weight ramp above. Lua / mainnet value
     * is `4`. MUST be > 0.
     */
    maxTenureWeight: number | bigint;
};
export declare function getInitializeEpochsInstructionDataEncoder(): FixedSizeEncoder<InitializeEpochsInstructionDataArgs>;
export declare function getInitializeEpochsInstructionDataDecoder(): FixedSizeDecoder<InitializeEpochsInstructionData>;
export declare function getInitializeEpochsInstructionDataCodec(): FixedSizeCodec<InitializeEpochsInstructionDataArgs, InitializeEpochsInstructionData>;
export type InitializeEpochsAsyncInput<TAccountEpochSettings extends string = string, TAccountPayer extends string = string, TAccountProgramData extends string = string, TAccountSystemProgram extends string = string> = {
    epochSettings?: Address<TAccountEpochSettings>;
    /**
     * Bind initialize_epochs to the program's upgrade authority — closes
     * the front-run window between `solana program deploy` and
     * `initialize_epochs` (audit H1 / Theme C). Explicit
     * `Some(payer.key())` equality rejects revoked upgrade authority.
     */
    payer: TransactionSigner<TAccountPayer>;
    programData?: Address<TAccountProgramData>;
    systemProgram?: Address<TAccountSystemProgram>;
    authority: InitializeEpochsInstructionDataArgs["authority"];
    epochDuration: InitializeEpochsInstructionDataArgs["epochDuration"];
    observerCount: InitializeEpochsInstructionDataArgs["observerCount"];
    nameCount: InitializeEpochsInstructionDataArgs["nameCount"];
    minObserverStake: InitializeEpochsInstructionDataArgs["minObserverStake"];
    slashRate: InitializeEpochsInstructionDataArgs["slashRate"];
    tenureWeightDuration: InitializeEpochsInstructionDataArgs["tenureWeightDuration"];
    maxTenureWeight: InitializeEpochsInstructionDataArgs["maxTenureWeight"];
};
export declare function getInitializeEpochsInstructionAsync<TAccountEpochSettings extends string, TAccountPayer extends string, TAccountProgramData extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: InitializeEpochsAsyncInput<TAccountEpochSettings, TAccountPayer, TAccountProgramData, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<InitializeEpochsInstruction<TProgramAddress, TAccountEpochSettings, TAccountPayer, TAccountProgramData, TAccountSystemProgram>>;
export type InitializeEpochsInput<TAccountEpochSettings extends string = string, TAccountPayer extends string = string, TAccountProgramData extends string = string, TAccountSystemProgram extends string = string> = {
    epochSettings: Address<TAccountEpochSettings>;
    /**
     * Bind initialize_epochs to the program's upgrade authority — closes
     * the front-run window between `solana program deploy` and
     * `initialize_epochs` (audit H1 / Theme C). Explicit
     * `Some(payer.key())` equality rejects revoked upgrade authority.
     */
    payer: TransactionSigner<TAccountPayer>;
    programData: Address<TAccountProgramData>;
    systemProgram?: Address<TAccountSystemProgram>;
    authority: InitializeEpochsInstructionDataArgs["authority"];
    epochDuration: InitializeEpochsInstructionDataArgs["epochDuration"];
    observerCount: InitializeEpochsInstructionDataArgs["observerCount"];
    nameCount: InitializeEpochsInstructionDataArgs["nameCount"];
    minObserverStake: InitializeEpochsInstructionDataArgs["minObserverStake"];
    slashRate: InitializeEpochsInstructionDataArgs["slashRate"];
    tenureWeightDuration: InitializeEpochsInstructionDataArgs["tenureWeightDuration"];
    maxTenureWeight: InitializeEpochsInstructionDataArgs["maxTenureWeight"];
};
export declare function getInitializeEpochsInstruction<TAccountEpochSettings extends string, TAccountPayer extends string, TAccountProgramData extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: InitializeEpochsInput<TAccountEpochSettings, TAccountPayer, TAccountProgramData, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): InitializeEpochsInstruction<TProgramAddress, TAccountEpochSettings, TAccountPayer, TAccountProgramData, TAccountSystemProgram>;
export type ParsedInitializeEpochsInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        epochSettings: TAccountMetas[0];
        /**
         * Bind initialize_epochs to the program's upgrade authority — closes
         * the front-run window between `solana program deploy` and
         * `initialize_epochs` (audit H1 / Theme C). Explicit
         * `Some(payer.key())` equality rejects revoked upgrade authority.
         */
        payer: TAccountMetas[1];
        programData: TAccountMetas[2];
        systemProgram: TAccountMetas[3];
    };
    data: InitializeEpochsInstructionData;
};
export declare function parseInitializeEpochsInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedInitializeEpochsInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=initializeEpochs.d.ts.map