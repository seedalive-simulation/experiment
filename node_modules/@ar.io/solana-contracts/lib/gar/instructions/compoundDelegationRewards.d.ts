/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type WritableAccount } from '@solana/kit';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const COMPOUND_DELEGATION_REWARDS_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getCompoundDelegationRewardsDiscriminatorBytes(): ReadonlyUint8Array;
export type CompoundDelegationRewardsInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountGateway extends string | AccountMeta<string> = string, TAccountDelegation extends string | AccountMeta<string> = string, TAccountDelegator extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountGateway extends string ? WritableAccount<TAccountGateway> : TAccountGateway, TAccountDelegation extends string ? WritableAccount<TAccountDelegation> : TAccountDelegation, TAccountDelegator extends string ? ReadonlyAccount<TAccountDelegator> : TAccountDelegator, ...TRemainingAccounts]>;
export type CompoundDelegationRewardsInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type CompoundDelegationRewardsInstructionDataArgs = {};
export declare function getCompoundDelegationRewardsInstructionDataEncoder(): FixedSizeEncoder<CompoundDelegationRewardsInstructionDataArgs>;
export declare function getCompoundDelegationRewardsInstructionDataDecoder(): FixedSizeDecoder<CompoundDelegationRewardsInstructionData>;
export declare function getCompoundDelegationRewardsInstructionDataCodec(): FixedSizeCodec<CompoundDelegationRewardsInstructionDataArgs, CompoundDelegationRewardsInstructionData>;
export type CompoundDelegationRewardsInput<TAccountGateway extends string = string, TAccountDelegation extends string = string, TAccountDelegator extends string = string> = {
    gateway: Address<TAccountGateway>;
    delegation: Address<TAccountDelegation>;
    /**
     * constraint pair above pins `delegation.delegator == delegator.key()`,
     * which is enforced cryptographically by the PDA derivation itself —
     * substituting a different pubkey produces a different Delegation PDA
     * that won't match `delegation`. No `Signer` constraint because this
     * instruction is permissionless per docs/INVARIANTS.md (audit M-2,
     * 2026-05-29): compounding only writes pending → active stake on a
     * delegate's own pre-existing balance — no funds move between
     * accounts, no authorization is needed. Permissionless invocation
     * enables the off-chain monitor's Invariant 1 health check and
     * unblocks `finalize_gone` for gateways with dormant pending rewards.
     */
    delegator: Address<TAccountDelegator>;
};
export declare function getCompoundDelegationRewardsInstruction<TAccountGateway extends string, TAccountDelegation extends string, TAccountDelegator extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: CompoundDelegationRewardsInput<TAccountGateway, TAccountDelegation, TAccountDelegator>, config?: {
    programAddress?: TProgramAddress;
}): CompoundDelegationRewardsInstruction<TProgramAddress, TAccountGateway, TAccountDelegation, TAccountDelegator>;
export type ParsedCompoundDelegationRewardsInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        gateway: TAccountMetas[0];
        delegation: TAccountMetas[1];
        /**
         * constraint pair above pins `delegation.delegator == delegator.key()`,
         * which is enforced cryptographically by the PDA derivation itself —
         * substituting a different pubkey produces a different Delegation PDA
         * that won't match `delegation`. No `Signer` constraint because this
         * instruction is permissionless per docs/INVARIANTS.md (audit M-2,
         * 2026-05-29): compounding only writes pending → active stake on a
         * delegate's own pre-existing balance — no funds move between
         * accounts, no authorization is needed. Permissionless invocation
         * enables the off-chain monitor's Invariant 1 health check and
         * unblocks `finalize_gone` for gateways with dormant pending rewards.
         */
        delegator: TAccountMetas[2];
    };
    data: CompoundDelegationRewardsInstructionData;
};
export declare function parseCompoundDelegationRewardsInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCompoundDelegationRewardsInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=compoundDelegationRewards.d.ts.map