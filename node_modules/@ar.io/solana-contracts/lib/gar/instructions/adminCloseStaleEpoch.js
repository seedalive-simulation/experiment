/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getNonNullResolvedInstructionInput } from '@solana/program-client-core';
import { findEpochPda, findEpochSettingsPda, findSettingsPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const ADMIN_CLOSE_STALE_EPOCH_DISCRIMINATOR = new Uint8Array([210, 30, 82, 29, 119, 159, 205, 129]);
export function getAdminCloseStaleEpochDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ADMIN_CLOSE_STALE_EPOCH_DISCRIMINATOR); }
export function getAdminCloseStaleEpochInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['epochIndex', getU64Encoder()]]), (value) => ({ ...value, discriminator: ADMIN_CLOSE_STALE_EPOCH_DISCRIMINATOR }));
}
export function getAdminCloseStaleEpochInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['epochIndex', getU64Decoder()]]);
}
export function getAdminCloseStaleEpochInstructionDataCodec() {
    return combineCodec(getAdminCloseStaleEpochInstructionDataEncoder(), getAdminCloseStaleEpochInstructionDataDecoder());
}
export async function getAdminCloseStaleEpochInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { settings: { value: input.settings ?? null, isWritable: false }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, epoch: { value: input.epoch ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.settings.value) {
        accounts.settings.value = await findSettingsPda();
    }
    if (!accounts.epochSettings.value) {
        accounts.epochSettings.value = await findEpochSettingsPda();
    }
    if (!accounts.epoch.value) {
        accounts.epoch.value = await findEpochPda({ epochIndex: getNonNullResolvedInstructionInput("epochIndex", args.epochIndex) }, { programAddress });
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("settings", accounts.settings), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("epoch", accounts.epoch), getAccountMeta("authority", accounts.authority)], data: getAdminCloseStaleEpochInstructionDataEncoder().encode(args), programAddress });
}
export function getAdminCloseStaleEpochInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { settings: { value: input.settings ?? null, isWritable: false }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, epoch: { value: input.epoch ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("settings", accounts.settings), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("epoch", accounts.epoch), getAccountMeta("authority", accounts.authority)], data: getAdminCloseStaleEpochInstructionDataEncoder().encode(args), programAddress });
}
export function parseAdminCloseStaleEpochInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { settings: getNextAccount(), epochSettings: getNextAccount(), epoch: getNextAccount(), authority: getNextAccount() }, data: getAdminCloseStaleEpochInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=adminCloseStaleEpoch.js.map