/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findEpochSettingsPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../programs/index.js';
export const ADMIN_SET_REWARD_RATIOS_DISCRIMINATOR = new Uint8Array([117, 102, 171, 158, 81, 97, 6, 208]);
export function getAdminSetRewardRatiosDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ADMIN_SET_REWARD_RATIOS_DISCRIMINATOR); }
export function getAdminSetRewardRatiosInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['gatewayRewardRatio', getU64Encoder()], ['observerRewardRatio', getU64Encoder()]]), (value) => ({ ...value, discriminator: ADMIN_SET_REWARD_RATIOS_DISCRIMINATOR }));
}
export function getAdminSetRewardRatiosInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['gatewayRewardRatio', getU64Decoder()], ['observerRewardRatio', getU64Decoder()]]);
}
export function getAdminSetRewardRatiosInstructionDataCodec() {
    return combineCodec(getAdminSetRewardRatiosInstructionDataEncoder(), getAdminSetRewardRatiosInstructionDataDecoder());
}
export async function getAdminSetRewardRatiosInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.epochSettings.value) {
        accounts.epochSettings.value = await findEpochSettingsPda();
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("authority", accounts.authority)], data: getAdminSetRewardRatiosInstructionDataEncoder().encode(args), programAddress });
}
export function getAdminSetRewardRatiosInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { epochSettings: { value: input.epochSettings ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("authority", accounts.authority)], data: getAdminSetRewardRatiosInstructionDataEncoder().encode(args), programAddress });
}
export function parseAdminSetRewardRatiosInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { epochSettings: getNextAccount(), authority: getNextAccount() }, data: getAdminSetRewardRatiosInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=adminSetRewardRatios.js.map