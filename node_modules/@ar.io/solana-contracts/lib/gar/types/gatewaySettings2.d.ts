/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type Codec, type Decoder, type Encoder, type Option, type OptionOrNullable } from '@solana/kit';
/** Gateway-specific settings (named differently to avoid conflict) */
export type GatewaySettings2 = {
    allowDelegatedStaking: boolean;
    delegateRewardShareRatio: number;
    minDelegationAmount: bigint;
    /**
     * When true, only delegates on the allowlist (or existing stakers) can delegate
     * Matches Lua: allowedDelegatesLookup ~= nil
     */
    allowlistEnabled: boolean;
    /**
     * Fix #7 (WP §6.3): a `delegate_reward_share_ratio` change requested via
     * `update_gateway_settings` during an epoch is staged here and applied at
     * the next `tally_weights` (start of the following epoch), so an operator
     * can't front-run `distribute_epoch` to change their reward split. `None`
     * when no change is pending. Stored in basis points (0-10000), same scale
     * as `delegate_reward_share_ratio`.
     */
    pendingDelegateRewardShareRatio: Option<number>;
    /**
     * Fix #6 (WP §6.3): timestamp (seconds) when the operator disabled
     * `allow_delegated_staking`. Starts the cooldown that blocks re-enabling
     * until all delegates have been withdrawn AND `settings.withdrawal_period`
     * has elapsed. `None` when delegation is enabled (or was never disabled).
     */
    delegationDisabledAt: Option<bigint>;
};
export type GatewaySettings2Args = {
    allowDelegatedStaking: boolean;
    delegateRewardShareRatio: number;
    minDelegationAmount: number | bigint;
    /**
     * When true, only delegates on the allowlist (or existing stakers) can delegate
     * Matches Lua: allowedDelegatesLookup ~= nil
     */
    allowlistEnabled: boolean;
    /**
     * Fix #7 (WP §6.3): a `delegate_reward_share_ratio` change requested via
     * `update_gateway_settings` during an epoch is staged here and applied at
     * the next `tally_weights` (start of the following epoch), so an operator
     * can't front-run `distribute_epoch` to change their reward split. `None`
     * when no change is pending. Stored in basis points (0-10000), same scale
     * as `delegate_reward_share_ratio`.
     */
    pendingDelegateRewardShareRatio: OptionOrNullable<number>;
    /**
     * Fix #6 (WP §6.3): timestamp (seconds) when the operator disabled
     * `allow_delegated_staking`. Starts the cooldown that blocks re-enabling
     * until all delegates have been withdrawn AND `settings.withdrawal_period`
     * has elapsed. `None` when delegation is enabled (or was never disabled).
     */
    delegationDisabledAt: OptionOrNullable<number | bigint>;
};
export declare function getGatewaySettings2Encoder(): Encoder<GatewaySettings2Args>;
export declare function getGatewaySettings2Decoder(): Decoder<GatewaySettings2>;
export declare function getGatewaySettings2Codec(): Codec<GatewaySettings2Args, GatewaySettings2>;
//# sourceMappingURL=gatewaySettings2.d.ts.map