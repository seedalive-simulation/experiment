/**
 * AUTOGENERATED by clients/ts/scripts/codegen.mjs — do not edit by hand.
 *
 * Program constants lifted from the Rust source-of-truth. These do NOT
 * appear in the Anchor IDL, so Codama cannot generate them; re-run
 * `yarn codegen` to refresh after changing the Rust consts.
 */
/**
 * Scale factor for fixed-point math (1.0 = 1_000_000)
 *
 * Rust: `DEMAND_FACTOR_SCALE: u64` (src/state/mod.rs)
 */
export declare const DEMAND_FACTOR_SCALE: bigint;
/**
 * Minimum demand factor (0.5)
 *
 * Rust: `DEMAND_FACTOR_MIN: u64` (src/state/mod.rs)
 */
export declare const DEMAND_FACTOR_MIN: bigint;
/**
 * Maximum demand factor (1000.0) — a CU-safety ceiling on the 1.05x
 * up-adjustment. The Lua reference (`demand.lua`) does NOT cap the factor;
 * this is a Solana-only bound (see BD-112) that keeps the lazy-rollover
 * fast-forward bounded: a long-idle decay back to the floor takes at most
 * ~510 steps from this ceiling, vs. unbounded growth otherwise. Set far above
 * any economically reachable value (1000x base fees would require ~141
 * consecutive periods of geometrically-rising demand), so it never affects
 * realistic pricing — it only removes a pathological compute cliff.
 *
 * Rust: `MAX_DEMAND_FACTOR: u64` (src/state/mod.rs)
 */
export declare const MAX_DEMAND_FACTOR: bigint;
/**
 * Upward adjustment multiplier (1.05x)
 *
 * Rust: `DEMAND_FACTOR_UP_ADJUSTMENT: u64` (src/state/mod.rs)
 */
export declare const DEMAND_FACTOR_UP_ADJUSTMENT: bigint;
/**
 * Downward adjustment multiplier (0.985x)
 *
 * Rust: `DEMAND_FACTOR_DOWN_ADJUSTMENT: u64` (src/state/mod.rs)
 */
export declare const DEMAND_FACTOR_DOWN_ADJUSTMENT: bigint;
/**
 * After this many consecutive periods at min, fees are permanently halved
 *
 * Rust: `MAX_PERIODS_AT_MIN_DEMAND_FACTOR: u32` (src/state/mod.rs)
 */
export declare const MAX_PERIODS_AT_MIN_DEMAND_FACTOR: number;
/**
 * Number of trailing periods used for moving average
 *
 * Rust: `MOVING_AVG_PERIOD_COUNT: usize` (src/state/mod.rs)
 */
export declare const MOVING_AVG_PERIOD_COUNT: number;
/**
 * Duration of one demand-factor period (1 day)
 *
 * Rust: `PERIOD_LENGTH_SECONDS: i64` (src/state/mod.rs)
 */
export declare const PERIOD_LENGTH_SECONDS: bigint;
/**
 * Annual percentage fee for lease renewals (0.2 scaled)
 *
 * Rust: `ANNUAL_PERCENTAGE_FEE: u64` (src/state/mod.rs)
 */
export declare const ANNUAL_PERCENTAGE_FEE: bigint;
/**
 * Equivalent lease length in years for permabuy pricing
 *
 * Rust: `PERMABUY_LEASE_FEE_LENGTH_YEARS: u64` (src/state/mod.rs)
 */
export declare const PERMABUY_LEASE_FEE_LENGTH_YEARS: bigint;
/**
 * Undername fee as fraction of registration (lease, 0.001 scaled)
 *
 * Rust: `UNDERNAME_LEASE_FEE_PCT: u64` (src/state/mod.rs)
 */
export declare const UNDERNAME_LEASE_FEE_PCT: bigint;
/**
 * Undername fee as fraction of registration (permabuy, 0.005 scaled)
 *
 * Rust: `UNDERNAME_PERMABUY_FEE_PCT: u64` (src/state/mod.rs)
 */
export declare const UNDERNAME_PERMABUY_FEE_PCT: bigint;
/**
 * Grace period after lease expiry before name can be reclaimed (14 days)
 *
 * Rust: `GRACE_PERIOD_SECONDS: i64` (src/state/mod.rs)
 */
export declare const GRACE_PERIOD_SECONDS: bigint;
/**
 * Duration of a returned-name auction (14 days)
 *
 * Rust: `RETURNED_NAME_DURATION_SECONDS: i64` (src/state/mod.rs)
 */
export declare const RETURNED_NAME_DURATION_SECONDS: bigint;
/**
 * Maximum premium multiplier at the start of a returned-name auction
 *
 * Rust: `RETURNED_NAME_MAX_MULTIPLIER: u64` (src/state/mod.rs)
 */
export declare const RETURNED_NAME_MAX_MULTIPLIER: bigint;
/**
 * Maximum lease duration
 *
 * Rust: `MAX_LEASE_LENGTH_YEARS: u8` (src/state/mod.rs)
 */
export declare const MAX_LEASE_LENGTH_YEARS: number;
//# sourceMappingURL=constants.d.ts.map