/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findConfigPda } from '../pdas/index.js';
import { ARIO_ARNS_PROGRAM_ADDRESS } from '../programs/index.js';
export const UNRESERVE_NAME_DISCRIMINATOR = new Uint8Array([145, 177, 75, 128, 243, 180, 105, 253]);
export function getUnreserveNameDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(UNRESERVE_NAME_DISCRIMINATOR); }
export function getUnreserveNameInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: UNRESERVE_NAME_DISCRIMINATOR }));
}
export function getUnreserveNameInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getUnreserveNameInstructionDataCodec() {
    return combineCodec(getUnreserveNameInstructionDataEncoder(), getUnreserveNameInstructionDataDecoder());
}
export async function getUnreserveNameInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ARNS_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, reservedName: { value: input.reservedName ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda();
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("reservedName", accounts.reservedName), getAccountMeta("authority", accounts.authority)], data: getUnreserveNameInstructionDataEncoder().encode({}), programAddress });
}
export function getUnreserveNameInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ARNS_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, reservedName: { value: input.reservedName ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("reservedName", accounts.reservedName), getAccountMeta("authority", accounts.authority)], data: getUnreserveNameInstructionDataEncoder().encode({}), programAddress });
}
export function parseUnreserveNameInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { config: getNextAccount(), reservedName: getNextAccount(), authority: getNextAccount() }, data: getUnreserveNameInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=unreserveName.js.map