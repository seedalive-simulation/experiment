/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findConfigPda } from '../pdas/index.js';
import { ARIO_ARNS_PROGRAM_ADDRESS } from '../programs/index.js';
export const REASSIGN_NAME_DISCRIMINATOR = new Uint8Array([115, 226, 169, 53, 243, 247, 40, 16]);
export function getReassignNameDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(REASSIGN_NAME_DISCRIMINATOR); }
export function getReassignNameInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['newAnt', getAddressEncoder()]]), (value) => ({ ...value, discriminator: REASSIGN_NAME_DISCRIMINATOR }));
}
export function getReassignNameInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['newAnt', getAddressDecoder()]]);
}
export function getReassignNameInstructionDataCodec() {
    return combineCodec(getReassignNameInstructionDataEncoder(), getReassignNameInstructionDataDecoder());
}
export async function getReassignNameInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ARNS_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, arnsRecord: { value: input.arnsRecord ?? null, isWritable: true }, antAsset: { value: input.antAsset ?? null, isWritable: false }, caller: { value: input.caller ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda();
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("arnsRecord", accounts.arnsRecord), getAccountMeta("antAsset", accounts.antAsset), getAccountMeta("caller", accounts.caller), getAccountMeta("systemProgram", accounts.systemProgram)], data: getReassignNameInstructionDataEncoder().encode(args), programAddress });
}
export function getReassignNameInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ARNS_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, arnsRecord: { value: input.arnsRecord ?? null, isWritable: true }, antAsset: { value: input.antAsset ?? null, isWritable: false }, caller: { value: input.caller ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("arnsRecord", accounts.arnsRecord), getAccountMeta("antAsset", accounts.antAsset), getAccountMeta("caller", accounts.caller), getAccountMeta("systemProgram", accounts.systemProgram)], data: getReassignNameInstructionDataEncoder().encode(args), programAddress });
}
export function parseReassignNameInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { config: getNextAccount(), arnsRecord: getNextAccount(), antAsset: getNextAccount(), caller: getNextAccount(), systemProgram: getNextAccount() }, data: getReassignNameInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=reassignName.js.map