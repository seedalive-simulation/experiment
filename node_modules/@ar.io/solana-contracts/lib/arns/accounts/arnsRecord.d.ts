/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type Option, type OptionOrNullable, type ReadonlyUint8Array } from '@solana/kit';
import { type PurchaseType, type PurchaseTypeArgs, type SchemaVersion, type SchemaVersionArgs } from '../types/index.js';
export declare const ARNS_RECORD_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getArnsRecordDiscriminatorBytes(): ReadonlyUint8Array;
export type ArnsRecord = {
    discriminator: ReadonlyUint8Array;
    /** SHA256 hash of lowercase name (used for PDA derivation) */
    nameHash: ReadonlyUint8Array;
    /**
     * Current owner. NOTE (ISSUES.md, Minor): set at purchase, never
     * updated, never read on-chain. Slated for removal — kept here
     * for now to avoid a second schema break.
     */
    owner: Address;
    /**
     * Associated ANT (Metaplex NFT mint). Authoritative for all
     * "who controls this name" decisions — flows through MPL Core
     * asset ownership + `AntControllers`.
     */
    ant: Address;
    /** Lease or Permabuy */
    purchaseType: PurchaseType;
    /** When the name was purchased (seconds) */
    startTimestamp: bigint;
    /** When the lease expires (seconds); None for permabuy */
    endTimestamp: Option<bigint>;
    /** Maximum number of undernames */
    undernameLimit: number;
    /** Price paid at purchase (mARIO) */
    purchasePrice: bigint;
    /** PDA bump */
    bump: number;
    /**
     * The name string (max 51 chars, original casing preserved).
     * Variable-length. The memcmp-filtered fixed fields (`owner` @40,
     * `ant` @72, …) all precede it, so their offsets stay stable.
     */
    name: string;
    /**
     * Schema version for forward-compatible migrations. MUST be the
     * byte-end (last) field: it follows the variable-length `name` so a
     * pre-versioning record can be grown + zero-filled and read as
     * {0,0,0} → bootstrap. Reordered here from before `name` (which made
     * old records unloadable). See `schema_migration::grow_account`.
     */
    version: SchemaVersion;
};
export type ArnsRecordArgs = {
    /** SHA256 hash of lowercase name (used for PDA derivation) */
    nameHash: ReadonlyUint8Array;
    /**
     * Current owner. NOTE (ISSUES.md, Minor): set at purchase, never
     * updated, never read on-chain. Slated for removal — kept here
     * for now to avoid a second schema break.
     */
    owner: Address;
    /**
     * Associated ANT (Metaplex NFT mint). Authoritative for all
     * "who controls this name" decisions — flows through MPL Core
     * asset ownership + `AntControllers`.
     */
    ant: Address;
    /** Lease or Permabuy */
    purchaseType: PurchaseTypeArgs;
    /** When the name was purchased (seconds) */
    startTimestamp: number | bigint;
    /** When the lease expires (seconds); None for permabuy */
    endTimestamp: OptionOrNullable<number | bigint>;
    /** Maximum number of undernames */
    undernameLimit: number;
    /** Price paid at purchase (mARIO) */
    purchasePrice: number | bigint;
    /** PDA bump */
    bump: number;
    /**
     * The name string (max 51 chars, original casing preserved).
     * Variable-length. The memcmp-filtered fixed fields (`owner` @40,
     * `ant` @72, …) all precede it, so their offsets stay stable.
     */
    name: string;
    /**
     * Schema version for forward-compatible migrations. MUST be the
     * byte-end (last) field: it follows the variable-length `name` so a
     * pre-versioning record can be grown + zero-filled and read as
     * {0,0,0} → bootstrap. Reordered here from before `name` (which made
     * old records unloadable). See `schema_migration::grow_account`.
     */
    version: SchemaVersionArgs;
};
/** Gets the encoder for {@link ArnsRecordArgs} account data. */
export declare function getArnsRecordEncoder(): Encoder<ArnsRecordArgs>;
/** Gets the decoder for {@link ArnsRecord} account data. */
export declare function getArnsRecordDecoder(): Decoder<ArnsRecord>;
/** Gets the codec for {@link ArnsRecord} account data. */
export declare function getArnsRecordCodec(): Codec<ArnsRecordArgs, ArnsRecord>;
export declare function decodeArnsRecord<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<ArnsRecord, TAddress>;
export declare function decodeArnsRecord<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<ArnsRecord, TAddress>;
export declare function fetchArnsRecord<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<ArnsRecord, TAddress>>;
export declare function fetchMaybeArnsRecord<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<ArnsRecord, TAddress>>;
export declare function fetchAllArnsRecord(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<ArnsRecord>[]>;
export declare function fetchAllMaybeArnsRecord(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<ArnsRecord>[]>;
//# sourceMappingURL=arnsRecord.d.ts.map