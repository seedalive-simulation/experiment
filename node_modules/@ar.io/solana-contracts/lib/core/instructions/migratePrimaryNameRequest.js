/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findRequestPda } from '../pdas/index.js';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_PRIMARY_NAME_REQUEST_DISCRIMINATOR = new Uint8Array([113, 151, 253, 243, 208, 242, 119, 186]);
export function getMigratePrimaryNameRequestDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_PRIMARY_NAME_REQUEST_DISCRIMINATOR); }
export function getMigratePrimaryNameRequestInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: MIGRATE_PRIMARY_NAME_REQUEST_DISCRIMINATOR }));
}
export function getMigratePrimaryNameRequestInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getMigratePrimaryNameRequestInstructionDataCodec() {
    return combineCodec(getMigratePrimaryNameRequestInstructionDataEncoder(), getMigratePrimaryNameRequestInstructionDataDecoder());
}
export async function getMigratePrimaryNameRequestInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { request: { value: input.request ?? null, isWritable: true }, initiator: { value: input.initiator ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.request.value) {
        accounts.request.value = await findRequestPda({ initiator: getAddressFromResolvedInstructionAccount("initiator", accounts.initiator.value) }, { programAddress });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("request", accounts.request), getAccountMeta("initiator", accounts.initiator), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigratePrimaryNameRequestInstructionDataEncoder().encode({}), programAddress });
}
export function getMigratePrimaryNameRequestInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { request: { value: input.request ?? null, isWritable: true }, initiator: { value: input.initiator ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("request", accounts.request), getAccountMeta("initiator", accounts.initiator), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigratePrimaryNameRequestInstructionDataEncoder().encode({}), programAddress });
}
export function parseMigratePrimaryNameRequestInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { request: getNextAccount(), initiator: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() }, data: getMigratePrimaryNameRequestInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migratePrimaryNameRequest.js.map