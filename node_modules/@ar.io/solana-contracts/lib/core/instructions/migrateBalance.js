/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findBalancePda } from '../pdas/index.js';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_BALANCE_DISCRIMINATOR = new Uint8Array([29, 57, 219, 49, 7, 42, 88, 171]);
export function getMigrateBalanceDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_BALANCE_DISCRIMINATOR); }
export function getMigrateBalanceInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: MIGRATE_BALANCE_DISCRIMINATOR }));
}
export function getMigrateBalanceInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getMigrateBalanceInstructionDataCodec() {
    return combineCodec(getMigrateBalanceInstructionDataEncoder(), getMigrateBalanceInstructionDataDecoder());
}
export async function getMigrateBalanceInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { balance: { value: input.balance ?? null, isWritable: true }, owner: { value: input.owner ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.balance.value) {
        accounts.balance.value = await findBalancePda({ owner: getAddressFromResolvedInstructionAccount("owner", accounts.owner.value) }, { programAddress });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("balance", accounts.balance), getAccountMeta("owner", accounts.owner), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateBalanceInstructionDataEncoder().encode({}), programAddress });
}
export function getMigrateBalanceInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { balance: { value: input.balance ?? null, isWritable: true }, owner: { value: input.owner ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("balance", accounts.balance), getAccountMeta("owner", accounts.owner), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateBalanceInstructionDataEncoder().encode({}), programAddress });
}
export function parseMigrateBalanceInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { balance: getNextAccount(), owner: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() }, data: getMigrateBalanceInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migrateBalance.js.map