/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findConfigPda } from '../pdas/index.js';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../programs/index.js';
export const RELEASE_TREASURY_TO_RECIPIENT_DISCRIMINATOR = new Uint8Array([254, 14, 125, 6, 252, 223, 78, 8]);
export function getReleaseTreasuryToRecipientDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(RELEASE_TREASURY_TO_RECIPIENT_DISCRIMINATOR); }
export function getReleaseTreasuryToRecipientInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['amount', getU64Encoder()]]), (value) => ({ ...value, discriminator: RELEASE_TREASURY_TO_RECIPIENT_DISCRIMINATOR }));
}
export function getReleaseTreasuryToRecipientInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['amount', getU64Decoder()]]);
}
export function getReleaseTreasuryToRecipientInstructionDataCodec() {
    return combineCodec(getReleaseTreasuryToRecipientInstructionDataEncoder(), getReleaseTreasuryToRecipientInstructionDataDecoder());
}
export async function getReleaseTreasuryToRecipientInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, garSettings: { value: input.garSettings ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda();
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("garSettings", accounts.garSettings), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getReleaseTreasuryToRecipientInstructionDataEncoder().encode(args), programAddress });
}
export function getReleaseTreasuryToRecipientInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, garSettings: { value: input.garSettings ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("garSettings", accounts.garSettings), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getReleaseTreasuryToRecipientInstructionDataEncoder().encode(args), programAddress });
}
export function parseReleaseTreasuryToRecipientInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { config: getNextAccount(), protocolTokenAccount: getNextAccount(), recipientTokenAccount: getNextAccount(), garSettings: getNextAccount(), tokenProgram: getNextAccount() }, data: getReleaseTreasuryToRecipientInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=releaseTreasuryToRecipient.js.map