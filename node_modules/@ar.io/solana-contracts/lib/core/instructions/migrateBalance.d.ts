/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const MIGRATE_BALANCE_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getMigrateBalanceDiscriminatorBytes(): ReadonlyUint8Array;
export type MigrateBalanceInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountBalance extends string | AccountMeta<string> = string, TAccountOwner extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountBalance extends string ? WritableAccount<TAccountBalance> : TAccountBalance, TAccountOwner extends string ? ReadonlyAccount<TAccountOwner> : TAccountOwner, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type MigrateBalanceInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type MigrateBalanceInstructionDataArgs = {};
export declare function getMigrateBalanceInstructionDataEncoder(): FixedSizeEncoder<MigrateBalanceInstructionDataArgs>;
export declare function getMigrateBalanceInstructionDataDecoder(): FixedSizeDecoder<MigrateBalanceInstructionData>;
export declare function getMigrateBalanceInstructionDataCodec(): FixedSizeCodec<MigrateBalanceInstructionDataArgs, MigrateBalanceInstructionData>;
export type MigrateBalanceAsyncInput<TAccountBalance extends string = string, TAccountOwner extends string = string, TAccountPayer extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * `UncheckedAccount` (not `Account<Balance>`) so a pre-versioning,
   * shorter account can be grown by `grow_account` BEFORE deserialization
   * — see `schema_migration::grow_account`. The handler grows, then
   * borsh-deserializes `Balance` and re-serializes it.
   */
    balance?: Address<TAccountBalance>;
    owner: Address<TAccountOwner>;
    payer: TransactionSigner<TAccountPayer>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getMigrateBalanceInstructionAsync<TAccountBalance extends string, TAccountOwner extends string, TAccountPayer extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: MigrateBalanceAsyncInput<TAccountBalance, TAccountOwner, TAccountPayer, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<MigrateBalanceInstruction<TProgramAddress, TAccountBalance, TAccountOwner, TAccountPayer, TAccountSystemProgram>>;
export type MigrateBalanceInput<TAccountBalance extends string = string, TAccountOwner extends string = string, TAccountPayer extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * `UncheckedAccount` (not `Account<Balance>`) so a pre-versioning,
   * shorter account can be grown by `grow_account` BEFORE deserialization
   * — see `schema_migration::grow_account`. The handler grows, then
   * borsh-deserializes `Balance` and re-serializes it.
   */
    balance: Address<TAccountBalance>;
    owner: Address<TAccountOwner>;
    payer: TransactionSigner<TAccountPayer>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getMigrateBalanceInstruction<TAccountBalance extends string, TAccountOwner extends string, TAccountPayer extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: MigrateBalanceInput<TAccountBalance, TAccountOwner, TAccountPayer, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): MigrateBalanceInstruction<TProgramAddress, TAccountBalance, TAccountOwner, TAccountPayer, TAccountSystemProgram>;
export type ParsedMigrateBalanceInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * `UncheckedAccount` (not `Account<Balance>`) so a pre-versioning,
         * shorter account can be grown by `grow_account` BEFORE deserialization
         * — see `schema_migration::grow_account`. The handler grows, then
         * borsh-deserializes `Balance` and re-serializes it.
         */
        balance: TAccountMetas[0];
        owner: TAccountMetas[1];
        payer: TAccountMetas[2];
        systemProgram: TAccountMetas[3];
    };
    data: MigrateBalanceInstructionData;
};
export declare function parseMigrateBalanceInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedMigrateBalanceInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=migrateBalance.d.ts.map