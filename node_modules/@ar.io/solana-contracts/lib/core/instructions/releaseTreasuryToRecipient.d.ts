/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const RELEASE_TREASURY_TO_RECIPIENT_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getReleaseTreasuryToRecipientDiscriminatorBytes(): ReadonlyUint8Array;
export type ReleaseTreasuryToRecipientInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountConfig extends string | AccountMeta<string> = string, TAccountProtocolTokenAccount extends string | AccountMeta<string> = string, TAccountRecipientTokenAccount extends string | AccountMeta<string> = string, TAccountGarSettings extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountConfig extends string ? ReadonlyAccount<TAccountConfig> : TAccountConfig, TAccountProtocolTokenAccount extends string ? WritableAccount<TAccountProtocolTokenAccount> : TAccountProtocolTokenAccount, TAccountRecipientTokenAccount extends string ? WritableAccount<TAccountRecipientTokenAccount> : TAccountRecipientTokenAccount, TAccountGarSettings extends string ? ReadonlySignerAccount<TAccountGarSettings> & AccountSignerMeta<TAccountGarSettings> : TAccountGarSettings, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, ...TRemainingAccounts]>;
export type ReleaseTreasuryToRecipientInstructionData = {
    discriminator: ReadonlyUint8Array;
    amount: bigint;
};
export type ReleaseTreasuryToRecipientInstructionDataArgs = {
    amount: number | bigint;
};
export declare function getReleaseTreasuryToRecipientInstructionDataEncoder(): FixedSizeEncoder<ReleaseTreasuryToRecipientInstructionDataArgs>;
export declare function getReleaseTreasuryToRecipientInstructionDataDecoder(): FixedSizeDecoder<ReleaseTreasuryToRecipientInstructionData>;
export declare function getReleaseTreasuryToRecipientInstructionDataCodec(): FixedSizeCodec<ReleaseTreasuryToRecipientInstructionDataArgs, ReleaseTreasuryToRecipientInstructionData>;
export type ReleaseTreasuryToRecipientAsyncInput<TAccountConfig extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountGarSettings extends string = string, TAccountTokenProgram extends string = string> = {
    /** ArioConfig PDA. Signs the SPL transfer as treasury authority. */
    config?: Address<TAccountConfig>;
    /**
     * Source: the protocol treasury. Address pinned via
     * `ArioConfig.treasury` so callers can't substitute a different
     * token account.
     */
    protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
    /**
     * Destination: locked to GAR's stake_token_account. Even if a
     * compromised or buggy GAR build calls this ix with wild values,
     * the funds can only flow into GAR's own staking pool.
     */
    recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
    /**
     * Cross-program signer proof. Three things must hold for this
     * account to be accepted:
     * * `signer` — the call carries a signature for this account.
     * Only the GAR program can produce one (via `invoke_signed`
     * with `SETTINGS_SEED`); Solana's runtime enforces that PDA
     * signatures can only originate from the program owning them.
     * * `seeds = [b"gar_settings"]` + `seeds::program = config.gar_program`
     * — the account is the canonical gar_settings PDA derived
     * from the GAR program ID stored in `ArioConfig`. Storing the
     * program ID in config (vs. hardcoding `ario_gar::ID`) lets
     * operators rotate the trusted GAR program post-deploy
     * without an ario-core rebuild — same flexibility we have
     * for `arns_program`. Operators are still trusted (they hold
     * `authority`), so this isn't a security regression.
     * * `Account<'info, GatewaySettings>` — Anchor type-deserializes
     * and verifies the account is owned by ario-gar (the
     * `#[account]` discriminator includes the declaring crate's
     * ID).
     *
     * Together these prove: the call originated from the GAR program
     * referenced by `config.gar_program`, not from a third party
     * impersonating gar_settings as a regular account.
     */
    garSettings: TransactionSigner<TAccountGarSettings>;
    tokenProgram?: Address<TAccountTokenProgram>;
    amount: ReleaseTreasuryToRecipientInstructionDataArgs["amount"];
};
export declare function getReleaseTreasuryToRecipientInstructionAsync<TAccountConfig extends string, TAccountProtocolTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountGarSettings extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: ReleaseTreasuryToRecipientAsyncInput<TAccountConfig, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountGarSettings, TAccountTokenProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<ReleaseTreasuryToRecipientInstruction<TProgramAddress, TAccountConfig, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountGarSettings, TAccountTokenProgram>>;
export type ReleaseTreasuryToRecipientInput<TAccountConfig extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountGarSettings extends string = string, TAccountTokenProgram extends string = string> = {
    /** ArioConfig PDA. Signs the SPL transfer as treasury authority. */
    config: Address<TAccountConfig>;
    /**
     * Source: the protocol treasury. Address pinned via
     * `ArioConfig.treasury` so callers can't substitute a different
     * token account.
     */
    protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
    /**
     * Destination: locked to GAR's stake_token_account. Even if a
     * compromised or buggy GAR build calls this ix with wild values,
     * the funds can only flow into GAR's own staking pool.
     */
    recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
    /**
     * Cross-program signer proof. Three things must hold for this
     * account to be accepted:
     * * `signer` — the call carries a signature for this account.
     * Only the GAR program can produce one (via `invoke_signed`
     * with `SETTINGS_SEED`); Solana's runtime enforces that PDA
     * signatures can only originate from the program owning them.
     * * `seeds = [b"gar_settings"]` + `seeds::program = config.gar_program`
     * — the account is the canonical gar_settings PDA derived
     * from the GAR program ID stored in `ArioConfig`. Storing the
     * program ID in config (vs. hardcoding `ario_gar::ID`) lets
     * operators rotate the trusted GAR program post-deploy
     * without an ario-core rebuild — same flexibility we have
     * for `arns_program`. Operators are still trusted (they hold
     * `authority`), so this isn't a security regression.
     * * `Account<'info, GatewaySettings>` — Anchor type-deserializes
     * and verifies the account is owned by ario-gar (the
     * `#[account]` discriminator includes the declaring crate's
     * ID).
     *
     * Together these prove: the call originated from the GAR program
     * referenced by `config.gar_program`, not from a third party
     * impersonating gar_settings as a regular account.
     */
    garSettings: TransactionSigner<TAccountGarSettings>;
    tokenProgram?: Address<TAccountTokenProgram>;
    amount: ReleaseTreasuryToRecipientInstructionDataArgs["amount"];
};
export declare function getReleaseTreasuryToRecipientInstruction<TAccountConfig extends string, TAccountProtocolTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountGarSettings extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: ReleaseTreasuryToRecipientInput<TAccountConfig, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountGarSettings, TAccountTokenProgram>, config?: {
    programAddress?: TProgramAddress;
}): ReleaseTreasuryToRecipientInstruction<TProgramAddress, TAccountConfig, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountGarSettings, TAccountTokenProgram>;
export type ParsedReleaseTreasuryToRecipientInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** ArioConfig PDA. Signs the SPL transfer as treasury authority. */
        config: TAccountMetas[0];
        /**
         * Source: the protocol treasury. Address pinned via
         * `ArioConfig.treasury` so callers can't substitute a different
         * token account.
         */
        protocolTokenAccount: TAccountMetas[1];
        /**
         * Destination: locked to GAR's stake_token_account. Even if a
         * compromised or buggy GAR build calls this ix with wild values,
         * the funds can only flow into GAR's own staking pool.
         */
        recipientTokenAccount: TAccountMetas[2];
        /**
         * Cross-program signer proof. Three things must hold for this
         * account to be accepted:
         * * `signer` — the call carries a signature for this account.
         * Only the GAR program can produce one (via `invoke_signed`
         * with `SETTINGS_SEED`); Solana's runtime enforces that PDA
         * signatures can only originate from the program owning them.
         * * `seeds = [b"gar_settings"]` + `seeds::program = config.gar_program`
         * — the account is the canonical gar_settings PDA derived
         * from the GAR program ID stored in `ArioConfig`. Storing the
         * program ID in config (vs. hardcoding `ario_gar::ID`) lets
         * operators rotate the trusted GAR program post-deploy
         * without an ario-core rebuild — same flexibility we have
         * for `arns_program`. Operators are still trusted (they hold
         * `authority`), so this isn't a security regression.
         * * `Account<'info, GatewaySettings>` — Anchor type-deserializes
         * and verifies the account is owned by ario-gar (the
         * `#[account]` discriminator includes the declaring crate's
         * ID).
         *
         * Together these prove: the call originated from the GAR program
         * referenced by `config.gar_program`, not from a third party
         * impersonating gar_settings as a regular account.
         */
        garSettings: TAccountMetas[3];
        tokenProgram: TAccountMetas[4];
    };
    data: ReleaseTreasuryToRecipientInstructionData;
};
export declare function parseReleaseTreasuryToRecipientInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedReleaseTreasuryToRecipientInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=releaseTreasuryToRecipient.d.ts.map