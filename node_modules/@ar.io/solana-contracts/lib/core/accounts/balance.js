/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getArioCoreStateSchemaVersionDecoder, getArioCoreStateSchemaVersionEncoder } from '../types/index.js';
export const BALANCE_DISCRIMINATOR = new Uint8Array([127, 71, 25, 157, 105, 157, 241, 182]);
export function getBalanceDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(BALANCE_DISCRIMINATOR); }
/** Gets the encoder for {@link BalanceArgs} account data. */
export function getBalanceEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['owner', getAddressEncoder()], ['amount', getU64Encoder()], ['bump', getU8Encoder()], ['version', getArioCoreStateSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: BALANCE_DISCRIMINATOR }));
}
/** Gets the decoder for {@link Balance} account data. */
export function getBalanceDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['owner', getAddressDecoder()], ['amount', getU64Decoder()], ['bump', getU8Decoder()], ['version', getArioCoreStateSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link Balance} account data. */
export function getBalanceCodec() {
    return combineCodec(getBalanceEncoder(), getBalanceDecoder());
}
export function decodeBalance(encodedAccount) {
    return decodeAccount(encodedAccount, getBalanceDecoder());
}
export async function fetchBalance(rpc, address, config) {
    const maybeAccount = await fetchMaybeBalance(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeBalance(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeBalance(maybeAccount);
}
export async function fetchAllBalance(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeBalance(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeBalance(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeBalance(maybeAccount));
}
export function getBalanceSize() {
    return 52;
}
//# sourceMappingURL=balance.js.map