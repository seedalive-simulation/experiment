/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getOptionDecoder, getOptionEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getArioCoreStateSchemaVersionDecoder, getArioCoreStateSchemaVersionEncoder } from '../types/index.js';
export const VAULT_DISCRIMINATOR = new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]);
export function getVaultDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DISCRIMINATOR); }
/** Gets the encoder for {@link VaultArgs} account data. */
export function getVaultEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['owner', getAddressEncoder()], ['vaultId', getU64Encoder()], ['amount', getU64Encoder()], ['startTimestamp', getI64Encoder()], ['endTimestamp', getI64Encoder()], ['controller', getOptionEncoder(getAddressEncoder())], ['revocable', getBooleanEncoder()], ['bump', getU8Encoder()], ['version', getArioCoreStateSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: VAULT_DISCRIMINATOR }));
}
/** Gets the decoder for {@link Vault} account data. */
export function getVaultDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['owner', getAddressDecoder()], ['vaultId', getU64Decoder()], ['amount', getU64Decoder()], ['startTimestamp', getI64Decoder()], ['endTimestamp', getI64Decoder()], ['controller', getOptionDecoder(getAddressDecoder())], ['revocable', getBooleanDecoder()], ['bump', getU8Decoder()], ['version', getArioCoreStateSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link Vault} account data. */
export function getVaultCodec() {
    return combineCodec(getVaultEncoder(), getVaultDecoder());
}
export function decodeVault(encodedAccount) {
    return decodeAccount(encodedAccount, getVaultDecoder());
}
export async function fetchVault(rpc, address, config) {
    const maybeAccount = await fetchMaybeVault(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeVault(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeVault(maybeAccount);
}
export async function fetchAllVault(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeVault(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeVault(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeVault(maybeAccount));
}
//# sourceMappingURL=vault.js.map