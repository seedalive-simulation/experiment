/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const CLAIM_ANT_ARWEAVE_ATTESTED_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getClaimAntArweaveAttestedDiscriminatorBytes(): ReadonlyUint8Array;
export type ClaimAntArweaveAttestedInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountEscrow extends string | AccountMeta<string> = string, TAccountAntAsset extends string | AccountMeta<string> = string, TAccountClaimant extends string | AccountMeta<string> = string, TAccountDepositor extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountInstructionsSysvar extends string | AccountMeta<string> = "Sysvar1nstructions1111111111111111111111111", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountAntAsset extends string ? WritableAccount<TAccountAntAsset> : TAccountAntAsset, TAccountClaimant extends string ? ReadonlyAccount<TAccountClaimant> : TAccountClaimant, TAccountDepositor extends string ? WritableAccount<TAccountDepositor> : TAccountDepositor, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountInstructionsSysvar extends string ? ReadonlyAccount<TAccountInstructionsSysvar> : TAccountInstructionsSysvar, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type ClaimAntArweaveAttestedInstructionData = {
    discriminator: ReadonlyUint8Array;
    messageNonce: ReadonlyUint8Array;
};
export type ClaimAntArweaveAttestedInstructionDataArgs = {
    messageNonce: ReadonlyUint8Array;
};
export declare function getClaimAntArweaveAttestedInstructionDataEncoder(): FixedSizeEncoder<ClaimAntArweaveAttestedInstructionDataArgs>;
export declare function getClaimAntArweaveAttestedInstructionDataDecoder(): FixedSizeDecoder<ClaimAntArweaveAttestedInstructionData>;
export declare function getClaimAntArweaveAttestedInstructionDataCodec(): FixedSizeCodec<ClaimAntArweaveAttestedInstructionDataArgs, ClaimAntArweaveAttestedInstructionData>;
export type ClaimAntArweaveAttestedInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountClaimant extends string = string, TAccountDepositor extends string = string, TAccountPayer extends string = string, TAccountMplCoreProgram extends string = string, TAccountInstructionsSysvar extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * Escrow PDA. `has_one = depositor` ties the close-rent recipient
   * to the original depositor account passed below; `close = depositor`
   * returns rent on successful claim.
   */
    escrow: Address<TAccountEscrow>;
    /**
     * The Metaplex Core asset being released. Must match
     * `escrow.ant_mint` so a misrouted account can't redirect the
     * transfer.
     */
    antAsset: Address<TAccountAntAsset>;
    /**
     * Recipient of the ANT — its pubkey is bound into the canonical
     * message that the attestor signed over. Front-runners can resubmit
     * the tx but cannot redirect the asset.
     */
    claimant: Address<TAccountClaimant>;
    /**
     * Original depositor — receives rent on escrow close. Pinned via
     * `has_one = depositor` on the escrow above.
     */
    depositor: Address<TAccountDepositor>;
    /**
     * Tx fee payer. Doesn't have to be the claimant — anyone can submit
     * a valid attestation on the recipient's behalf.
     */
    payer: TransactionSigner<TAccountPayer>;
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    /**
     * Solana `sysvar::instructions` — required for introspecting the
     * preceding Ed25519Program sigverify instruction.
     */
    instructionsSysvar?: Address<TAccountInstructionsSysvar>;
    systemProgram?: Address<TAccountSystemProgram>;
    messageNonce: ClaimAntArweaveAttestedInstructionDataArgs["messageNonce"];
};
export declare function getClaimAntArweaveAttestedInstruction<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountClaimant extends string, TAccountDepositor extends string, TAccountPayer extends string, TAccountMplCoreProgram extends string, TAccountInstructionsSysvar extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: ClaimAntArweaveAttestedInput<TAccountEscrow, TAccountAntAsset, TAccountClaimant, TAccountDepositor, TAccountPayer, TAccountMplCoreProgram, TAccountInstructionsSysvar, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): ClaimAntArweaveAttestedInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountClaimant, TAccountDepositor, TAccountPayer, TAccountMplCoreProgram, TAccountInstructionsSysvar, TAccountSystemProgram>;
export type ParsedClaimAntArweaveAttestedInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * Escrow PDA. `has_one = depositor` ties the close-rent recipient
         * to the original depositor account passed below; `close = depositor`
         * returns rent on successful claim.
         */
        escrow: TAccountMetas[0];
        /**
         * The Metaplex Core asset being released. Must match
         * `escrow.ant_mint` so a misrouted account can't redirect the
         * transfer.
         */
        antAsset: TAccountMetas[1];
        /**
         * Recipient of the ANT — its pubkey is bound into the canonical
         * message that the attestor signed over. Front-runners can resubmit
         * the tx but cannot redirect the asset.
         */
        claimant: TAccountMetas[2];
        /**
         * Original depositor — receives rent on escrow close. Pinned via
         * `has_one = depositor` on the escrow above.
         */
        depositor: TAccountMetas[3];
        /**
         * Tx fee payer. Doesn't have to be the claimant — anyone can submit
         * a valid attestation on the recipient's behalf.
         */
        payer: TAccountMetas[4];
        mplCoreProgram: TAccountMetas[5];
        /**
         * Solana `sysvar::instructions` — required for introspecting the
         * preceding Ed25519Program sigverify instruction.
         */
        instructionsSysvar: TAccountMetas[6];
        systemProgram: TAccountMetas[7];
    };
    data: ClaimAntArweaveAttestedInstructionData;
};
export declare function parseClaimAntArweaveAttestedInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedClaimAntArweaveAttestedInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=claimAntArweaveAttested.d.ts.map