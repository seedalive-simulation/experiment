/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type Codec, type Decoder, type Encoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const UPDATE_RECIPIENT_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getUpdateRecipientDiscriminatorBytes(): ReadonlyUint8Array;
export type UpdateRecipientInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountEscrow extends string | AccountMeta<string> = string, TAccountDepositor extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountDepositor extends string ? ReadonlySignerAccount<TAccountDepositor> & AccountSignerMeta<TAccountDepositor> : TAccountDepositor, ...TRemainingAccounts]>;
export type UpdateRecipientInstructionData = {
    discriminator: ReadonlyUint8Array;
    newProtocol: number;
    newPubkey: ReadonlyUint8Array;
};
export type UpdateRecipientInstructionDataArgs = {
    newProtocol: number;
    newPubkey: ReadonlyUint8Array;
};
export declare function getUpdateRecipientInstructionDataEncoder(): Encoder<UpdateRecipientInstructionDataArgs>;
export declare function getUpdateRecipientInstructionDataDecoder(): Decoder<UpdateRecipientInstructionData>;
export declare function getUpdateRecipientInstructionDataCodec(): Codec<UpdateRecipientInstructionDataArgs, UpdateRecipientInstructionData>;
export type UpdateRecipientInput<TAccountEscrow extends string = string, TAccountDepositor extends string = string> = {
    /**
   * Escrow being re-targeted. `has_one = depositor` enforces the
   * authority check and the `seeds`/`bump = escrow.bump` constraint
   * re-derives the PDA against the cached bump (cheaper than
   * `find_program_address`).
   */
    escrow: Address<TAccountEscrow>;
    /** Depositor — sole authority for `update_recipient`. */
    depositor: TransactionSigner<TAccountDepositor>;
    newProtocol: UpdateRecipientInstructionDataArgs["newProtocol"];
    newPubkey: UpdateRecipientInstructionDataArgs["newPubkey"];
};
export declare function getUpdateRecipientInstruction<TAccountEscrow extends string, TAccountDepositor extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: UpdateRecipientInput<TAccountEscrow, TAccountDepositor>, config?: {
    programAddress?: TProgramAddress;
}): UpdateRecipientInstruction<TProgramAddress, TAccountEscrow, TAccountDepositor>;
export type ParsedUpdateRecipientInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * Escrow being re-targeted. `has_one = depositor` enforces the
         * authority check and the `seeds`/`bump = escrow.bump` constraint
         * re-derives the PDA against the cached bump (cheaper than
         * `find_program_address`).
         */
        escrow: TAccountMetas[0];
        /** Depositor — sole authority for `update_recipient`. */
        depositor: TAccountMetas[1];
    };
    data: UpdateRecipientInstructionData;
};
export declare function parseUpdateRecipientInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedUpdateRecipientInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=updateRecipient.d.ts.map