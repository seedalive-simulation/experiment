/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, getNonNullResolvedInstructionInput } from '@solana/program-client-core';
import { findDepositTokensEscrowPda } from '../pdas/index.js';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_ESCROW_TOKEN_DISCRIMINATOR = new Uint8Array([3, 170, 70, 206, 98, 70, 26, 211]);
export function getMigrateEscrowTokenDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_ESCROW_TOKEN_DISCRIMINATOR); }
export function getMigrateEscrowTokenInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['assetId', fixEncoderSize(getBytesEncoder(), 32)]]), (value) => ({ ...value, discriminator: MIGRATE_ESCROW_TOKEN_DISCRIMINATOR }));
}
export function getMigrateEscrowTokenInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['assetId', fixDecoderSize(getBytesDecoder(), 32)]]);
}
export function getMigrateEscrowTokenInstructionDataCodec() {
    return combineCodec(getMigrateEscrowTokenInstructionDataEncoder(), getMigrateEscrowTokenInstructionDataDecoder());
}
export async function getMigrateEscrowTokenInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_ESCROW_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { depositor: { value: input.depositor ?? null, isWritable: false }, escrow: { value: input.escrow ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.escrow.value) {
        accounts.escrow.value = await findDepositTokensEscrowPda({ depositor: getAddressFromResolvedInstructionAccount("depositor", accounts.depositor.value), assetId: getNonNullResolvedInstructionInput("assetId", args.assetId) }, { programAddress });
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("depositor", accounts.depositor), getAccountMeta("escrow", accounts.escrow)], data: getMigrateEscrowTokenInstructionDataEncoder().encode(args), programAddress });
}
export function getMigrateEscrowTokenInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_ESCROW_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { depositor: { value: input.depositor ?? null, isWritable: false }, escrow: { value: input.escrow ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("depositor", accounts.depositor), getAccountMeta("escrow", accounts.escrow)], data: getMigrateEscrowTokenInstructionDataEncoder().encode(args), programAddress });
}
export function parseMigrateEscrowTokenInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { depositor: getNextAccount(), escrow: getNextAccount() }, data: getMigrateEscrowTokenInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migrateEscrowToken.js.map