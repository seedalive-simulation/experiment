/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const ADMIN_PURGE_UNCLAIMED_ANT_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getAdminPurgeUnclaimedAntDiscriminatorBytes(): ReadonlyUint8Array;
export type AdminPurgeUnclaimedAntInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountEscrow extends string | AccountMeta<string> = string, TAccountAntAsset extends string | AccountMeta<string> = string, TAccountArioConfig extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountAntAsset extends string ? WritableAccount<TAccountAntAsset> : TAccountAntAsset, TAccountArioConfig extends string ? ReadonlyAccount<TAccountArioConfig> : TAccountArioConfig, TAccountAuthority extends string ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type AdminPurgeUnclaimedAntInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type AdminPurgeUnclaimedAntInstructionDataArgs = {};
export declare function getAdminPurgeUnclaimedAntInstructionDataEncoder(): FixedSizeEncoder<AdminPurgeUnclaimedAntInstructionDataArgs>;
export declare function getAdminPurgeUnclaimedAntInstructionDataDecoder(): FixedSizeDecoder<AdminPurgeUnclaimedAntInstructionData>;
export declare function getAdminPurgeUnclaimedAntInstructionDataCodec(): FixedSizeCodec<AdminPurgeUnclaimedAntInstructionDataArgs, AdminPurgeUnclaimedAntInstructionData>;
export type AdminPurgeUnclaimedAntAsyncInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountArioConfig extends string = string, TAccountAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * Escrow being purged. `close = authority` returns its rent to the
   * admin on successful handler completion.
   */
    escrow: Address<TAccountEscrow>;
    /**
     * The Metaplex Core asset to burn.
     * the mpl-core program; the BurnV1 CPI further validates layout.
     */
    antAsset: Address<TAccountAntAsset>;
    /**
     * `ArioConfig` PDA from ario-core. Read-only — used to gate the
     * signer against the protocol authority. Cross-program account
     * reference via the `cpi` feature dependency in Cargo.toml.
     */
    arioConfig?: Address<TAccountArioConfig>;
    /**
     * Protocol admin — must equal `ario_config.authority`. The address
     * constraint enforces it; this is what makes the ix admin-only.
     */
    authority: TransactionSigner<TAccountAuthority>;
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getAdminPurgeUnclaimedAntInstructionAsync<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountArioConfig extends string, TAccountAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: AdminPurgeUnclaimedAntAsyncInput<TAccountEscrow, TAccountAntAsset, TAccountArioConfig, TAccountAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<AdminPurgeUnclaimedAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountArioConfig, TAccountAuthority, TAccountMplCoreProgram, TAccountSystemProgram>>;
export type AdminPurgeUnclaimedAntInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountArioConfig extends string = string, TAccountAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * Escrow being purged. `close = authority` returns its rent to the
   * admin on successful handler completion.
   */
    escrow: Address<TAccountEscrow>;
    /**
     * The Metaplex Core asset to burn.
     * the mpl-core program; the BurnV1 CPI further validates layout.
     */
    antAsset: Address<TAccountAntAsset>;
    /**
     * `ArioConfig` PDA from ario-core. Read-only — used to gate the
     * signer against the protocol authority. Cross-program account
     * reference via the `cpi` feature dependency in Cargo.toml.
     */
    arioConfig: Address<TAccountArioConfig>;
    /**
     * Protocol admin — must equal `ario_config.authority`. The address
     * constraint enforces it; this is what makes the ix admin-only.
     */
    authority: TransactionSigner<TAccountAuthority>;
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getAdminPurgeUnclaimedAntInstruction<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountArioConfig extends string, TAccountAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: AdminPurgeUnclaimedAntInput<TAccountEscrow, TAccountAntAsset, TAccountArioConfig, TAccountAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): AdminPurgeUnclaimedAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountArioConfig, TAccountAuthority, TAccountMplCoreProgram, TAccountSystemProgram>;
export type ParsedAdminPurgeUnclaimedAntInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * Escrow being purged. `close = authority` returns its rent to the
         * admin on successful handler completion.
         */
        escrow: TAccountMetas[0];
        /**
         * The Metaplex Core asset to burn.
         * the mpl-core program; the BurnV1 CPI further validates layout.
         */
        antAsset: TAccountMetas[1];
        /**
         * `ArioConfig` PDA from ario-core. Read-only — used to gate the
         * signer against the protocol authority. Cross-program account
         * reference via the `cpi` feature dependency in Cargo.toml.
         */
        arioConfig: TAccountMetas[2];
        /**
         * Protocol admin — must equal `ario_config.authority`. The address
         * constraint enforces it; this is what makes the ix admin-only.
         */
        authority: TAccountMetas[3];
        mplCoreProgram: TAccountMetas[4];
        systemProgram: TAccountMetas[5];
    };
    data: AdminPurgeUnclaimedAntInstructionData;
};
export declare function parseAdminPurgeUnclaimedAntInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedAdminPurgeUnclaimedAntInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=adminPurgeUnclaimedAnt.d.ts.map