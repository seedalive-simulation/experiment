/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { type ArioCoreStateSchemaVersion, type ArioCoreStateSchemaVersionArgs } from '../types/index.js';
export declare const ARIO_CONFIG_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getArioConfigDiscriminatorBytes(): ReadonlyUint8Array;
export type ArioConfig = {
    discriminator: ReadonlyUint8Array;
    /** Admin authority for protocol updates */
    authority: Address;
    /** SPL token mint for ARIO token */
    mint: Address;
    /** ArNS program ID (for cross-program validation) */
    arnsProgram: Address;
    /** Treasury token account for fee collection */
    treasury: Address;
    /** Total token supply (fixed at creation) */
    totalSupply: bigint;
    /** Protocol treasury balance */
    protocolBalance: bigint;
    /** Circulating supply (total - locked - staked) */
    circulatingSupply: bigint;
    /** Amount locked in vaults */
    lockedSupply: bigint;
    /** Minimum vault lock duration (seconds) */
    minVaultDuration: bigint;
    /** Maximum vault lock duration (seconds) */
    maxVaultDuration: bigint;
    /** Primary name request expiry (seconds) */
    primaryNameRequestExpiry: bigint;
    /** Whether migration import is active (permanently disabled by finalize_migration) */
    migrationActive: boolean;
    /** Dedicated migration authority (hot key for batch imports) */
    migrationAuthority: Address;
    /** PDA bump */
    bump: number;
    /**
     * GAR program ID. Used by `release_treasury_to_recipient` to verify
     * the cross-program signer (`gar_settings`) was derived from the
     * canonical GAR program. Mirrors the `arns_program` storage
     * pattern. **Appended after `bump` for backward-compatibility:**
     * fresh deploys via `initialize` populate it directly; pre-existing
     * deployments populate via `admin_set_gar_program` (which reallocs
     * the account and writes the field). On a pre-realloc account this
     * field reads as `Pubkey::default()`.
     */
    garProgram: Address;
    /** Schema version for forward-compatible migrations. */
    version: ArioCoreStateSchemaVersion;
};
export type ArioConfigArgs = {
    /** Admin authority for protocol updates */
    authority: Address;
    /** SPL token mint for ARIO token */
    mint: Address;
    /** ArNS program ID (for cross-program validation) */
    arnsProgram: Address;
    /** Treasury token account for fee collection */
    treasury: Address;
    /** Total token supply (fixed at creation) */
    totalSupply: number | bigint;
    /** Protocol treasury balance */
    protocolBalance: number | bigint;
    /** Circulating supply (total - locked - staked) */
    circulatingSupply: number | bigint;
    /** Amount locked in vaults */
    lockedSupply: number | bigint;
    /** Minimum vault lock duration (seconds) */
    minVaultDuration: number | bigint;
    /** Maximum vault lock duration (seconds) */
    maxVaultDuration: number | bigint;
    /** Primary name request expiry (seconds) */
    primaryNameRequestExpiry: number | bigint;
    /** Whether migration import is active (permanently disabled by finalize_migration) */
    migrationActive: boolean;
    /** Dedicated migration authority (hot key for batch imports) */
    migrationAuthority: Address;
    /** PDA bump */
    bump: number;
    /**
     * GAR program ID. Used by `release_treasury_to_recipient` to verify
     * the cross-program signer (`gar_settings`) was derived from the
     * canonical GAR program. Mirrors the `arns_program` storage
     * pattern. **Appended after `bump` for backward-compatibility:**
     * fresh deploys via `initialize` populate it directly; pre-existing
     * deployments populate via `admin_set_gar_program` (which reallocs
     * the account and writes the field). On a pre-realloc account this
     * field reads as `Pubkey::default()`.
     */
    garProgram: Address;
    /** Schema version for forward-compatible migrations. */
    version: ArioCoreStateSchemaVersionArgs;
};
/** Gets the encoder for {@link ArioConfigArgs} account data. */
export declare function getArioConfigEncoder(): FixedSizeEncoder<ArioConfigArgs>;
/** Gets the decoder for {@link ArioConfig} account data. */
export declare function getArioConfigDecoder(): FixedSizeDecoder<ArioConfig>;
/** Gets the codec for {@link ArioConfig} account data. */
export declare function getArioConfigCodec(): FixedSizeCodec<ArioConfigArgs, ArioConfig>;
export declare function decodeArioConfig<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<ArioConfig, TAddress>;
export declare function decodeArioConfig<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<ArioConfig, TAddress>;
export declare function fetchArioConfig<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<ArioConfig, TAddress>>;
export declare function fetchMaybeArioConfig<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<ArioConfig, TAddress>>;
export declare function fetchAllArioConfig(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<ArioConfig>[]>;
export declare function fetchAllMaybeArioConfig(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<ArioConfig>[]>;
export declare function getArioConfigSize(): number;
//# sourceMappingURL=arioConfig.d.ts.map