/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { fetchEncodedAccount, fetchEncodedAccounts, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { type ArioAntEscrowStateSchemaVersion, type ArioAntEscrowStateSchemaVersionArgs } from '../types/index.js';
export declare const ESCROW_TOKEN_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getEscrowTokenDiscriminatorBytes(): ReadonlyUint8Array;
export type EscrowToken = {
    discriminator: ReadonlyUint8Array;
    /** Schema version. v1 = `SchemaVersion::new(1, 0, 0)`. */
    version: ArioAntEscrowStateSchemaVersion;
    /** PDA bump. */
    bump: number;
    /** Wallet that deposited the tokens. */
    depositor: Address;
    /** `ASSET_TYPE_TOKEN` (1) or `ASSET_TYPE_VAULT` (2). */
    assetType: number;
    /** Amount of ARIO (mARIO) held in escrow. */
    amount: bigint;
    /** The ARIO SPL token mint. Validated at deposit time. */
    arioMint: Address;
    /** Client-supplied unique identifier for this escrow. Part of PDA seeds. */
    assetId: ReadonlyUint8Array;
    /** `0` = Arweave RSA-PSS-4096, `1` = Ethereum ECDSA secp256k1. */
    recipientProtocol: number;
    /** Active length of the recipient pubkey blob. */
    recipientPubkeyLen: number;
    /** Recipient identity bytes, zero-padded to 512. */
    recipientPubkey: ReadonlyUint8Array;
    /** Anti-replay nonce. */
    nonce: ReadonlyUint8Array;
    /** Slot at deposit. */
    depositSlot: bigint;
    /**
     * For vault escrows: the unix timestamp at which the vault unlocks.
     * Zero for liquid token escrows.
     */
    vaultEndTimestamp: bigint;
    /**
     * Reserved. Always `false`: `deposit_vault` rejects `revocable=true` and
     * claim re-locks are always non-revocable (the escrow has no field for a
     * legitimate revoker, so a revocable re-lock could only be controlled by
     * the unbound claim-tx payer — a theft vector). Kept for layout/ABI
     * stability. See ADR-021. (Also `false` for liquid token escrows.)
     */
    vaultRevocable: boolean;
    /**
     * Reserved for future fields.
     * Shrunk from 32 to 30 to absorb the SchemaVersion expansion (u8→3 bytes).
     */
    reserved: ReadonlyUint8Array;
};
export type EscrowTokenArgs = {
    /** Schema version. v1 = `SchemaVersion::new(1, 0, 0)`. */
    version: ArioAntEscrowStateSchemaVersionArgs;
    /** PDA bump. */
    bump: number;
    /** Wallet that deposited the tokens. */
    depositor: Address;
    /** `ASSET_TYPE_TOKEN` (1) or `ASSET_TYPE_VAULT` (2). */
    assetType: number;
    /** Amount of ARIO (mARIO) held in escrow. */
    amount: number | bigint;
    /** The ARIO SPL token mint. Validated at deposit time. */
    arioMint: Address;
    /** Client-supplied unique identifier for this escrow. Part of PDA seeds. */
    assetId: ReadonlyUint8Array;
    /** `0` = Arweave RSA-PSS-4096, `1` = Ethereum ECDSA secp256k1. */
    recipientProtocol: number;
    /** Active length of the recipient pubkey blob. */
    recipientPubkeyLen: number;
    /** Recipient identity bytes, zero-padded to 512. */
    recipientPubkey: ReadonlyUint8Array;
    /** Anti-replay nonce. */
    nonce: ReadonlyUint8Array;
    /** Slot at deposit. */
    depositSlot: number | bigint;
    /**
     * For vault escrows: the unix timestamp at which the vault unlocks.
     * Zero for liquid token escrows.
     */
    vaultEndTimestamp: number | bigint;
    /**
     * Reserved. Always `false`: `deposit_vault` rejects `revocable=true` and
     * claim re-locks are always non-revocable (the escrow has no field for a
     * legitimate revoker, so a revocable re-lock could only be controlled by
     * the unbound claim-tx payer — a theft vector). Kept for layout/ABI
     * stability. See ADR-021. (Also `false` for liquid token escrows.)
     */
    vaultRevocable: boolean;
    /**
     * Reserved for future fields.
     * Shrunk from 32 to 30 to absorb the SchemaVersion expansion (u8→3 bytes).
     */
    reserved: ReadonlyUint8Array;
};
/** Gets the encoder for {@link EscrowTokenArgs} account data. */
export declare function getEscrowTokenEncoder(): FixedSizeEncoder<EscrowTokenArgs>;
/** Gets the decoder for {@link EscrowToken} account data. */
export declare function getEscrowTokenDecoder(): FixedSizeDecoder<EscrowToken>;
/** Gets the codec for {@link EscrowToken} account data. */
export declare function getEscrowTokenCodec(): FixedSizeCodec<EscrowTokenArgs, EscrowToken>;
export declare function decodeEscrowToken<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<EscrowToken, TAddress>;
export declare function decodeEscrowToken<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<EscrowToken, TAddress>;
export declare function fetchEscrowToken<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<Account<EscrowToken, TAddress>>;
export declare function fetchMaybeEscrowToken<TAddress extends string = string>(rpc: Parameters<typeof fetchEncodedAccount>[0], address: Address<TAddress>, config?: FetchAccountConfig): Promise<MaybeAccount<EscrowToken, TAddress>>;
export declare function fetchAllEscrowToken(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<Account<EscrowToken>[]>;
export declare function fetchAllMaybeEscrowToken(rpc: Parameters<typeof fetchEncodedAccounts>[0], addresses: Array<Address>, config?: FetchAccountsConfig): Promise<MaybeAccount<EscrowToken>[]>;
export declare function getEscrowTokenSize(): number;
//# sourceMappingURL=escrowToken.d.ts.map