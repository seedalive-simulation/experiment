/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const ACL_CONFIG_DISCRIMINATOR = new Uint8Array([97, 141, 93, 29, 198, 104, 199, 110]);
export function getAclConfigDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ACL_CONFIG_DISCRIMINATOR); }
/** Gets the encoder for {@link AclConfigArgs} account data. */
export function getAclConfigEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['user', getAddressEncoder()], ['pageCount', getU64Encoder()], ['totalEntries', getU64Encoder()], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: ACL_CONFIG_DISCRIMINATOR }));
}
/** Gets the decoder for {@link AclConfig} account data. */
export function getAclConfigDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['user', getAddressDecoder()], ['pageCount', getU64Decoder()], ['totalEntries', getU64Decoder()], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link AclConfig} account data. */
export function getAclConfigCodec() {
    return combineCodec(getAclConfigEncoder(), getAclConfigDecoder());
}
export function decodeAclConfig(encodedAccount) {
    return decodeAccount(encodedAccount, getAclConfigDecoder());
}
export async function fetchAclConfig(rpc, address, config) {
    const maybeAccount = await fetchMaybeAclConfig(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAclConfig(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAclConfig(maybeAccount);
}
export async function fetchAllAclConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAclConfig(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAclConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAclConfig(maybeAccount));
}
export function getAclConfigSize() {
    return 60;
}
//# sourceMappingURL=aclConfig.js.map