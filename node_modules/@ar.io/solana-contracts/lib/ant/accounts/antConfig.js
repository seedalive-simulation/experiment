/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { addDecoderSizePrefix, addEncoderSizePrefix, assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU8Decoder, getU8Encoder, getUtf8Decoder, getUtf8Encoder, transformEncoder } from '@solana/kit';
import { getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const ANT_CONFIG_DISCRIMINATOR = new Uint8Array([253, 120, 91, 64, 41, 244, 234, 229]);
export function getAntConfigDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ANT_CONFIG_DISCRIMINATOR); }
/** Gets the encoder for {@link AntConfigArgs} account data. */
export function getAntConfigEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['mint', getAddressEncoder()], ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['ticker', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['logo', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['description', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['keywords', getArrayEncoder(addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()))], ['lastKnownOwner', getAddressEncoder()], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: ANT_CONFIG_DISCRIMINATOR }));
}
/** Gets the decoder for {@link AntConfig} account data. */
export function getAntConfigDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['mint', getAddressDecoder()], ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['ticker', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['logo', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['description', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['keywords', getArrayDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()))], ['lastKnownOwner', getAddressDecoder()], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link AntConfig} account data. */
export function getAntConfigCodec() {
    return combineCodec(getAntConfigEncoder(), getAntConfigDecoder());
}
export function decodeAntConfig(encodedAccount) {
    return decodeAccount(encodedAccount, getAntConfigDecoder());
}
export async function fetchAntConfig(rpc, address, config) {
    const maybeAccount = await fetchMaybeAntConfig(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAntConfig(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAntConfig(maybeAccount);
}
export async function fetchAllAntConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAntConfig(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAntConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAntConfig(maybeAccount));
}
//# sourceMappingURL=antConfig.js.map