/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { addDecoderSizePrefix, addEncoderSizePrefix, assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getOptionDecoder, getOptionEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU8Decoder, getU8Encoder, getUtf8Decoder, getUtf8Encoder, transformEncoder } from '@solana/kit';
import { getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const ANT_RECORD_DISCRIMINATOR = new Uint8Array([225, 94, 70, 240, 82, 45, 135, 81]);
export function getAntRecordDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ANT_RECORD_DISCRIMINATOR); }
/** Gets the encoder for {@link AntRecordArgs} account data. */
export function getAntRecordEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['mint', getAddressEncoder()], ['undername', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['target', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())], ['targetProtocol', getU8Encoder()], ['ttlSeconds', getU32Encoder()], ['priority', getOptionEncoder(getU32Encoder())], ['owner', getOptionEncoder(getAddressEncoder())], ['lastReconciledOwner', getAddressEncoder()], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: ANT_RECORD_DISCRIMINATOR }));
}
/** Gets the decoder for {@link AntRecord} account data. */
export function getAntRecordDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['mint', getAddressDecoder()], ['undername', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['target', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())], ['targetProtocol', getU8Decoder()], ['ttlSeconds', getU32Decoder()], ['priority', getOptionDecoder(getU32Decoder())], ['owner', getOptionDecoder(getAddressDecoder())], ['lastReconciledOwner', getAddressDecoder()], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link AntRecord} account data. */
export function getAntRecordCodec() {
    return combineCodec(getAntRecordEncoder(), getAntRecordDecoder());
}
export function decodeAntRecord(encodedAccount) {
    return decodeAccount(encodedAccount, getAntRecordDecoder());
}
export async function fetchAntRecord(rpc, address, config) {
    const maybeAccount = await fetchMaybeAntRecord(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAntRecord(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAntRecord(maybeAccount);
}
export async function fetchAllAntRecord(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAntRecord(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAntRecord(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAntRecord(maybeAccount));
}
//# sourceMappingURL=antRecord.js.map