/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getAclEntryDecoder, getAclEntryEncoder, getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const ACL_PAGE_DISCRIMINATOR = new Uint8Array([46, 41, 180, 70, 215, 201, 18, 3]);
export function getAclPageDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ACL_PAGE_DISCRIMINATOR); }
/** Gets the encoder for {@link AclPageArgs} account data. */
export function getAclPageEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['user', getAddressEncoder()], ['pageIdx', getU64Encoder()], ['entries', getArrayEncoder(getAclEntryEncoder())], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: ACL_PAGE_DISCRIMINATOR }));
}
/** Gets the decoder for {@link AclPage} account data. */
export function getAclPageDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['user', getAddressDecoder()], ['pageIdx', getU64Decoder()], ['entries', getArrayDecoder(getAclEntryDecoder())], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link AclPage} account data. */
export function getAclPageCodec() {
    return combineCodec(getAclPageEncoder(), getAclPageDecoder());
}
export function decodeAclPage(encodedAccount) {
    return decodeAccount(encodedAccount, getAclPageDecoder());
}
export async function fetchAclPage(rpc, address, config) {
    const maybeAccount = await fetchMaybeAclPage(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAclPage(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAclPage(maybeAccount);
}
export async function fetchAllAclPage(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAclPage(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAclPage(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAclPage(maybeAccount));
}
//# sourceMappingURL=aclPage.js.map