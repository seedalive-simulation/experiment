/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder } from '@solana/kit';
import { getSchemaVersionDecoder, getSchemaVersionEncoder } from '../types/index.js';
export const ANT_CONTROLLERS_DISCRIMINATOR = new Uint8Array([10, 134, 117, 149, 96, 198, 6, 212]);
export function getAntControllersDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ANT_CONTROLLERS_DISCRIMINATOR); }
/** Gets the encoder for {@link AntControllersArgs} account data. */
export function getAntControllersEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['mint', getAddressEncoder()], ['controllers', getArrayEncoder(getAddressEncoder())], ['bump', getU8Encoder()], ['version', getSchemaVersionEncoder()]]), (value) => ({ ...value, discriminator: ANT_CONTROLLERS_DISCRIMINATOR }));
}
/** Gets the decoder for {@link AntControllers} account data. */
export function getAntControllersDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['mint', getAddressDecoder()], ['controllers', getArrayDecoder(getAddressDecoder())], ['bump', getU8Decoder()], ['version', getSchemaVersionDecoder()]]);
}
/** Gets the codec for {@link AntControllers} account data. */
export function getAntControllersCodec() {
    return combineCodec(getAntControllersEncoder(), getAntControllersDecoder());
}
export function decodeAntControllers(encodedAccount) {
    return decodeAccount(encodedAccount, getAntControllersDecoder());
}
export async function fetchAntControllers(rpc, address, config) {
    const maybeAccount = await fetchMaybeAntControllers(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAntControllers(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAntControllers(maybeAccount);
}
export async function fetchAllAntControllers(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAntControllers(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAntControllers(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAntControllers(maybeAccount));
}
//# sourceMappingURL=antControllers.js.map