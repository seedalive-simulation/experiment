/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type Codec, type Decoder, type Encoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const SYNC_ATTRIBUTES_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getSyncAttributesDiscriminatorBytes(): ReadonlyUint8Array;
export type SyncAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountAntAuthority extends string | AccountMeta<string> = string, TAccountArnsRecord extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountAntAuthority extends string ? ReadonlyAccount<TAccountAntAuthority> : TAccountAntAuthority, TAccountArnsRecord extends string ? ReadonlyAccount<TAccountArnsRecord> : TAccountArnsRecord, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type SyncAttributesInstructionData = {
    discriminator: ReadonlyUint8Array;
    name: string;
};
export type SyncAttributesInstructionDataArgs = {
    name: string;
};
export declare function getSyncAttributesInstructionDataEncoder(): Encoder<SyncAttributesInstructionDataArgs>;
export declare function getSyncAttributesInstructionDataDecoder(): Decoder<SyncAttributesInstructionData>;
export declare function getSyncAttributesInstructionDataCodec(): Codec<SyncAttributesInstructionDataArgs, SyncAttributesInstructionData>;
export type SyncAttributesAsyncInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountAntAuthority extends string = string, TAccountArnsRecord extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /** The Metaplex Core asset whose Attributes plugin will be updated. */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPI may charge and triggers the sync.
     * PERMISSIONLESS for ADR-028 (program-controlled) ANTs — any caller may
     * sponsor. For legacy ANTs (plugin authority = `Owner`) it MUST equal the
     * current MPL Core owner (checked in the handler), and signs UpdatePluginV1
     * directly.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * ANTs this is the asset's UpdateAuthority (and, via the
     * `Authority::UpdateAuthority` plugin binding, the Attributes-plugin
     * authority); the program signs UpdatePluginV1 with it. Address is pinned
     * by the seeds — never read or written.
     */
    antAuthority?: Address<TAccountAntAuthority>;
    /** PDA seeds + `ant == asset.key()` are all validated in the handler. */
    arnsRecord: Address<TAccountArnsRecord>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for
     * the inner CPI.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
    name: SyncAttributesInstructionDataArgs["name"];
};
export declare function getSyncAttributesInstructionAsync<TAccountAsset extends string, TAccountPayer extends string, TAccountAntAuthority extends string, TAccountArnsRecord extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: SyncAttributesAsyncInput<TAccountAsset, TAccountPayer, TAccountAntAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<SyncAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAntAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>>;
export type SyncAttributesInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountAntAuthority extends string = string, TAccountArnsRecord extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /** The Metaplex Core asset whose Attributes plugin will be updated. */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPI may charge and triggers the sync.
     * PERMISSIONLESS for ADR-028 (program-controlled) ANTs — any caller may
     * sponsor. For legacy ANTs (plugin authority = `Owner`) it MUST equal the
     * current MPL Core owner (checked in the handler), and signs UpdatePluginV1
     * directly.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * ANTs this is the asset's UpdateAuthority (and, via the
     * `Authority::UpdateAuthority` plugin binding, the Attributes-plugin
     * authority); the program signs UpdatePluginV1 with it. Address is pinned
     * by the seeds — never read or written.
     */
    antAuthority: Address<TAccountAntAuthority>;
    /** PDA seeds + `ant == asset.key()` are all validated in the handler. */
    arnsRecord: Address<TAccountArnsRecord>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for
     * the inner CPI.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
    name: SyncAttributesInstructionDataArgs["name"];
};
export declare function getSyncAttributesInstruction<TAccountAsset extends string, TAccountPayer extends string, TAccountAntAuthority extends string, TAccountArnsRecord extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: SyncAttributesInput<TAccountAsset, TAccountPayer, TAccountAntAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): SyncAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAntAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>;
export type ParsedSyncAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The Metaplex Core asset whose Attributes plugin will be updated. */
        asset: TAccountMetas[0];
        /**
         * Pays for any rent the inner CPI may charge and triggers the sync.
         * PERMISSIONLESS for ADR-028 (program-controlled) ANTs — any caller may
         * sponsor. For legacy ANTs (plugin authority = `Owner`) it MUST equal the
         * current MPL Core owner (checked in the handler), and signs UpdatePluginV1
         * directly.
         */
        payer: TAccountMetas[1];
        /**
         * ANTs this is the asset's UpdateAuthority (and, via the
         * `Authority::UpdateAuthority` plugin binding, the Attributes-plugin
         * authority); the program signs UpdatePluginV1 with it. Address is pinned
         * by the seeds — never read or written.
         */
        antAuthority: TAccountMetas[2];
        /** PDA seeds + `ant == asset.key()` are all validated in the handler. */
        arnsRecord: TAccountMetas[3];
        /**
         * Metaplex Core program — pinned to the canonical ID; required for
         * the inner CPI.
         */
        mplCoreProgram: TAccountMetas[4];
        systemProgram: TAccountMetas[5];
    };
    data: SyncAttributesInstructionData;
};
export declare function parseSyncAttributesInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSyncAttributesInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=syncAttributes.d.ts.map