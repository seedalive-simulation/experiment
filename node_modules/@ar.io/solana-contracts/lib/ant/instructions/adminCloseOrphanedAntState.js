/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findAntConfigPda, findAntControllersPda, findMigrationConfigPda } from '../pdas/index.js';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const ADMIN_CLOSE_ORPHANED_ANT_STATE_DISCRIMINATOR = new Uint8Array([243, 122, 229, 109, 155, 53, 98, 149]);
export function getAdminCloseOrphanedAntStateDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(ADMIN_CLOSE_ORPHANED_ANT_STATE_DISCRIMINATOR); }
export function getAdminCloseOrphanedAntStateInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: ADMIN_CLOSE_ORPHANED_ANT_STATE_DISCRIMINATOR }));
}
export function getAdminCloseOrphanedAntStateInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getAdminCloseOrphanedAntStateInstructionDataCodec() {
    return combineCodec(getAdminCloseOrphanedAntStateInstructionDataEncoder(), getAdminCloseOrphanedAntStateInstructionDataDecoder());
}
export async function getAdminCloseOrphanedAntStateInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: false }, migrationConfig: { value: input.migrationConfig ?? null, isWritable: false }, config: { value: input.config ?? null, isWritable: true }, controllers: { value: input.controllers ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.migrationConfig.value) {
        accounts.migrationConfig.value = await findMigrationConfigPda();
    }
    if (!accounts.config.value) {
        accounts.config.value = await findAntConfigPda({ asset: getAddressFromResolvedInstructionAccount("asset", accounts.asset.value) }, { programAddress });
    }
    if (!accounts.controllers.value) {
        accounts.controllers.value = await findAntControllersPda({ asset: getAddressFromResolvedInstructionAccount("asset", accounts.asset.value) }, { programAddress });
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("migrationConfig", accounts.migrationConfig), getAccountMeta("config", accounts.config), getAccountMeta("controllers", accounts.controllers), getAccountMeta("authority", accounts.authority)], data: getAdminCloseOrphanedAntStateInstructionDataEncoder().encode({}), programAddress });
}
export function getAdminCloseOrphanedAntStateInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: false }, migrationConfig: { value: input.migrationConfig ?? null, isWritable: false }, config: { value: input.config ?? null, isWritable: true }, controllers: { value: input.controllers ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true } };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("migrationConfig", accounts.migrationConfig), getAccountMeta("config", accounts.config), getAccountMeta("controllers", accounts.controllers), getAccountMeta("authority", accounts.authority)], data: getAdminCloseOrphanedAntStateInstructionDataEncoder().encode({}), programAddress });
}
export function parseAdminCloseOrphanedAntStateInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { asset: getNextAccount(), migrationConfig: getNextAccount(), config: getNextAccount(), controllers: getNextAccount(), authority: getNextAccount() }, data: getAdminCloseOrphanedAntStateInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=adminCloseOrphanedAntState.js.map