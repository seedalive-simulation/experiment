/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { addDecoderSizePrefix, addEncoderSizePrefix, combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getUtf8Decoder, getUtf8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const CLOSE_ANT_RECORD_DISCRIMINATOR = new Uint8Array([39, 35, 218, 1, 147, 86, 36, 199]);
export function getCloseAntRecordDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ANT_RECORD_DISCRIMINATOR); }
export function getCloseAntRecordInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['undername', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())]]), (value) => ({ ...value, discriminator: CLOSE_ANT_RECORD_DISCRIMINATOR }));
}
export function getCloseAntRecordInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['undername', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())]]);
}
export function getCloseAntRecordInstructionDataCodec() {
    return combineCodec(getCloseAntRecordInstructionDataEncoder(), getCloseAntRecordInstructionDataDecoder());
}
export function getCloseAntRecordInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: false }, record: { value: input.record ?? null, isWritable: true }, caller: { value: input.caller ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("record", accounts.record), getAccountMeta("caller", accounts.caller)], data: getCloseAntRecordInstructionDataEncoder().encode(args), programAddress });
}
export function parseCloseAntRecordInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { asset: getNextAccount(), record: getNextAccount(), caller: getNextAccount() }, data: getCloseAntRecordInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=closeAntRecord.js.map