/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const CLEAR_ATTRIBUTES_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getClearAttributesDiscriminatorBytes(): ReadonlyUint8Array;
export type ClearAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountAntAuthority extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountAntAuthority extends string ? ReadonlyAccount<TAccountAntAuthority> : TAccountAntAuthority, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type ClearAttributesInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type ClearAttributesInstructionDataArgs = {};
export declare function getClearAttributesInstructionDataEncoder(): FixedSizeEncoder<ClearAttributesInstructionDataArgs>;
export declare function getClearAttributesInstructionDataDecoder(): FixedSizeDecoder<ClearAttributesInstructionData>;
export declare function getClearAttributesInstructionDataCodec(): FixedSizeCodec<ClearAttributesInstructionDataArgs, ClearAttributesInstructionData>;
export type ClearAttributesAsyncInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountAuthority extends string = string, TAccountAntAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /** The Metaplex Core asset whose Attributes plugin will be reset. */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPI may charge. Often the same key as
     * `authority` but kept separate so a sponsor can pay.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * Must equal the asset's current MPL Core owner. Clear is owner-gated in
     * both models (it removes live traits); the signer here authorizes the
     * operation, while for program-controlled ANTs the program signs the CPI
     * with `ant_authority`.
     */
    authority: TransactionSigner<TAccountAuthority>;
    /** ANTs the program signs UpdatePluginV1 with it. Address pinned by seeds. */
    antAuthority?: Address<TAccountAntAuthority>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for
     * the inner CPI.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getClearAttributesInstructionAsync<TAccountAsset extends string, TAccountPayer extends string, TAccountAuthority extends string, TAccountAntAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: ClearAttributesAsyncInput<TAccountAsset, TAccountPayer, TAccountAuthority, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<ClearAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAuthority, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>>;
export type ClearAttributesInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountAuthority extends string = string, TAccountAntAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /** The Metaplex Core asset whose Attributes plugin will be reset. */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPI may charge. Often the same key as
     * `authority` but kept separate so a sponsor can pay.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * Must equal the asset's current MPL Core owner. Clear is owner-gated in
     * both models (it removes live traits); the signer here authorizes the
     * operation, while for program-controlled ANTs the program signs the CPI
     * with `ant_authority`.
     */
    authority: TransactionSigner<TAccountAuthority>;
    /** ANTs the program signs UpdatePluginV1 with it. Address pinned by seeds. */
    antAuthority: Address<TAccountAntAuthority>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for
     * the inner CPI.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getClearAttributesInstruction<TAccountAsset extends string, TAccountPayer extends string, TAccountAuthority extends string, TAccountAntAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: ClearAttributesInput<TAccountAsset, TAccountPayer, TAccountAuthority, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): ClearAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAuthority, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>;
export type ParsedClearAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The Metaplex Core asset whose Attributes plugin will be reset. */
        asset: TAccountMetas[0];
        /**
         * Pays for any rent the inner CPI may charge. Often the same key as
         * `authority` but kept separate so a sponsor can pay.
         */
        payer: TAccountMetas[1];
        /**
         * Must equal the asset's current MPL Core owner. Clear is owner-gated in
         * both models (it removes live traits); the signer here authorizes the
         * operation, while for program-controlled ANTs the program signs the CPI
         * with `ant_authority`.
         */
        authority: TAccountMetas[2];
        /** ANTs the program signs UpdatePluginV1 with it. Address pinned by seeds. */
        antAuthority: TAccountMetas[3];
        /**
         * Metaplex Core program — pinned to the canonical ID; required for
         * the inner CPI.
         */
        mplCoreProgram: TAccountMetas[4];
        systemProgram: TAccountMetas[5];
    };
    data: ClearAttributesInstructionData;
};
export declare function parseClearAttributesInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedClearAttributesInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=clearAttributes.d.ts.map