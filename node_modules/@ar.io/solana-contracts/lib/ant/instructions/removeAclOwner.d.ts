/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const REMOVE_ACL_OWNER_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getRemoveAclOwnerDiscriminatorBytes(): ReadonlyUint8Array;
export type RemoveAclOwnerInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountAntConfig extends string | AccountMeta<string> = string, TAccountAclConfig extends string | AccountMeta<string> = string, TAccountAclPage extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? ReadonlyAccount<TAccountAsset> : TAccountAsset, TAccountAntConfig extends string ? ReadonlyAccount<TAccountAntConfig> : TAccountAntConfig, TAccountAclConfig extends string ? WritableAccount<TAccountAclConfig> : TAccountAclConfig, TAccountAclPage extends string ? WritableAccount<TAccountAclPage> : TAccountAclPage, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type RemoveAclOwnerInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type RemoveAclOwnerInstructionDataArgs = {};
export declare function getRemoveAclOwnerInstructionDataEncoder(): FixedSizeEncoder<RemoveAclOwnerInstructionDataArgs>;
export declare function getRemoveAclOwnerInstructionDataDecoder(): FixedSizeDecoder<RemoveAclOwnerInstructionData>;
export declare function getRemoveAclOwnerInstructionDataCodec(): FixedSizeCodec<RemoveAclOwnerInstructionDataArgs, RemoveAclOwnerInstructionData>;
export type RemoveAclOwnerAsyncInput<TAccountAsset extends string = string, TAccountAntConfig extends string = string, TAccountAclConfig extends string = string, TAccountAclPage extends string = string, TAccountPayer extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * `read_asset_owner`. Owner program is checked there as well. The
   * `ant_config` PDA below is seeded by `asset.key()`, so Anchor will
   * fail to load it for any Core asset that ario-ant did not initialize.
   */
    asset: Address<TAccountAsset>;
    /**
     * Proves `asset` is a genuine AR.IO ANT: `AntConfig` is created at mint
     * time and seeded by the asset key, so a non-ANT Core asset has no such
     * PDA and this constraint fails. Without it, the permissionless owner
     * path would let anyone record arbitrary Core assets owned by the user
     * into their ACL, spoofing the reverse index.
     */
    antConfig?: Address<TAccountAntConfig>;
    aclConfig: Address<TAccountAclConfig>;
    /** Caller-supplied page. Validated against `acl_config` in handler. */
    aclPage: Address<TAccountAclPage>;
    /**
     * Pays for realloc growth on append; receives refunds the program
     * chooses to issue (currently none — pages do not shrink on remove).
     */
    payer: TransactionSigner<TAccountPayer>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getRemoveAclOwnerInstructionAsync<TAccountAsset extends string, TAccountAntConfig extends string, TAccountAclConfig extends string, TAccountAclPage extends string, TAccountPayer extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: RemoveAclOwnerAsyncInput<TAccountAsset, TAccountAntConfig, TAccountAclConfig, TAccountAclPage, TAccountPayer, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<RemoveAclOwnerInstruction<TProgramAddress, TAccountAsset, TAccountAntConfig, TAccountAclConfig, TAccountAclPage, TAccountPayer, TAccountSystemProgram>>;
export type RemoveAclOwnerInput<TAccountAsset extends string = string, TAccountAntConfig extends string = string, TAccountAclConfig extends string = string, TAccountAclPage extends string = string, TAccountPayer extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * `read_asset_owner`. Owner program is checked there as well. The
   * `ant_config` PDA below is seeded by `asset.key()`, so Anchor will
   * fail to load it for any Core asset that ario-ant did not initialize.
   */
    asset: Address<TAccountAsset>;
    /**
     * Proves `asset` is a genuine AR.IO ANT: `AntConfig` is created at mint
     * time and seeded by the asset key, so a non-ANT Core asset has no such
     * PDA and this constraint fails. Without it, the permissionless owner
     * path would let anyone record arbitrary Core assets owned by the user
     * into their ACL, spoofing the reverse index.
     */
    antConfig: Address<TAccountAntConfig>;
    aclConfig: Address<TAccountAclConfig>;
    /** Caller-supplied page. Validated against `acl_config` in handler. */
    aclPage: Address<TAccountAclPage>;
    /**
     * Pays for realloc growth on append; receives refunds the program
     * chooses to issue (currently none — pages do not shrink on remove).
     */
    payer: TransactionSigner<TAccountPayer>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getRemoveAclOwnerInstruction<TAccountAsset extends string, TAccountAntConfig extends string, TAccountAclConfig extends string, TAccountAclPage extends string, TAccountPayer extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: RemoveAclOwnerInput<TAccountAsset, TAccountAntConfig, TAccountAclConfig, TAccountAclPage, TAccountPayer, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): RemoveAclOwnerInstruction<TProgramAddress, TAccountAsset, TAccountAntConfig, TAccountAclConfig, TAccountAclPage, TAccountPayer, TAccountSystemProgram>;
export type ParsedRemoveAclOwnerInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * `read_asset_owner`. Owner program is checked there as well. The
         * `ant_config` PDA below is seeded by `asset.key()`, so Anchor will
         * fail to load it for any Core asset that ario-ant did not initialize.
         */
        asset: TAccountMetas[0];
        /**
         * Proves `asset` is a genuine AR.IO ANT: `AntConfig` is created at mint
         * time and seeded by the asset key, so a non-ANT Core asset has no such
         * PDA and this constraint fails. Without it, the permissionless owner
         * path would let anyone record arbitrary Core assets owned by the user
         * into their ACL, spoofing the reverse index.
         */
        antConfig: TAccountMetas[1];
        aclConfig: TAccountMetas[2];
        /** Caller-supplied page. Validated against `acl_config` in handler. */
        aclPage: TAccountMetas[3];
        /**
         * Pays for realloc growth on append; receives refunds the program
         * chooses to issue (currently none — pages do not shrink on remove).
         */
        payer: TAccountMetas[4];
        systemProgram: TAccountMetas[5];
    };
    data: RemoveAclOwnerInstructionData;
};
export declare function parseRemoveAclOwnerInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedRemoveAclOwnerInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=removeAclOwner.d.ts.map