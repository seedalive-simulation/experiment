/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const CLOSE_ACL_CONFIG_DISCRIMINATOR = new Uint8Array([106, 152, 171, 1, 57, 148, 88, 117]);
export function getCloseAclConfigDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ACL_CONFIG_DISCRIMINATOR); }
export function getCloseAclConfigInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_ACL_CONFIG_DISCRIMINATOR }));
}
export function getCloseAclConfigInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getCloseAclConfigInstructionDataCodec() {
    return combineCodec(getCloseAclConfigInstructionDataEncoder(), getCloseAclConfigInstructionDataDecoder());
}
export function getCloseAclConfigInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { aclConfig: { value: input.aclConfig ?? null, isWritable: true }, beneficiary: { value: input.beneficiary ?? null, isWritable: true } };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("aclConfig", accounts.aclConfig), getAccountMeta("beneficiary", accounts.beneficiary)], data: getCloseAclConfigInstructionDataEncoder().encode({}), programAddress });
}
export function parseCloseAclConfigInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { aclConfig: getNextAccount(), beneficiary: getNextAccount() }, data: getCloseAclConfigInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=closeAclConfig.js.map