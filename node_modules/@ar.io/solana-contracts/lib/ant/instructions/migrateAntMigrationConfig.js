/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory } from '@solana/program-client-core';
import { findMigrationConfigPda } from '../pdas/index.js';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const MIGRATE_ANT_MIGRATION_CONFIG_DISCRIMINATOR = new Uint8Array([61, 174, 102, 88, 95, 124, 207, 6]);
export function getMigrateAntMigrationConfigDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MIGRATE_ANT_MIGRATION_CONFIG_DISCRIMINATOR); }
export function getMigrateAntMigrationConfigInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: MIGRATE_ANT_MIGRATION_CONFIG_DISCRIMINATOR }));
}
export function getMigrateAntMigrationConfigInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getMigrateAntMigrationConfigInstructionDataCodec() {
    return combineCodec(getMigrateAntMigrationConfigInstructionDataEncoder(), getMigrateAntMigrationConfigInstructionDataDecoder());
}
export async function getMigrateAntMigrationConfigInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { migrationConfig: { value: input.migrationConfig ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.migrationConfig.value) {
        accounts.migrationConfig.value = await findMigrationConfigPda();
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("migrationConfig", accounts.migrationConfig), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateAntMigrationConfigInstructionDataEncoder().encode({}), programAddress });
}
export function getMigrateAntMigrationConfigInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { migrationConfig: { value: input.migrationConfig ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("migrationConfig", accounts.migrationConfig), getAccountMeta("payer", accounts.payer), getAccountMeta("systemProgram", accounts.systemProgram)], data: getMigrateAntMigrationConfigInstructionDataEncoder().encode({}), programAddress });
}
export function parseMigrateAntMigrationConfigInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { migrationConfig: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() }, data: getMigrateAntMigrationConfigInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=migrateAntMigrationConfig.js.map