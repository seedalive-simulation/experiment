/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const ADOPT_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getAdoptAuthorityDiscriminatorBytes(): ReadonlyUint8Array;
export type AdoptAuthorityInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountOwner extends string | AccountMeta<string> = string, TAccountAntAuthority extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountOwner extends string ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner> : TAccountOwner, TAccountAntAuthority extends string ? ReadonlyAccount<TAccountAntAuthority> : TAccountAntAuthority, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;
export type AdoptAuthorityInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type AdoptAuthorityInstructionDataArgs = {};
export declare function getAdoptAuthorityInstructionDataEncoder(): FixedSizeEncoder<AdoptAuthorityInstructionDataArgs>;
export declare function getAdoptAuthorityInstructionDataDecoder(): FixedSizeDecoder<AdoptAuthorityInstructionData>;
export declare function getAdoptAuthorityInstructionDataCodec(): FixedSizeCodec<AdoptAuthorityInstructionDataArgs, AdoptAuthorityInstructionData>;
export type AdoptAuthorityAsyncInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountOwner extends string = string, TAccountAntAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * The Metaplex Core asset being adopted.
   * from raw data in the handler.
   */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPIs may charge. Often the same key as
     * `owner` but kept separate so a sponsor can pay.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * Must equal the asset's current MPL Core owner. On legacy ANTs Owner ==
     * UpdateAuthority == plugin authority, so this one signature authorizes both
     * the RevokePluginAuthorityV1 and the UpdateV1 UA rotation.
     */
    owner: TransactionSigner<TAccountOwner>;
    /** rotation. Address pinned by seeds; never read or written directly. */
    antAuthority?: Address<TAccountAntAuthority>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for the
     * inner CPIs.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getAdoptAuthorityInstructionAsync<TAccountAsset extends string, TAccountPayer extends string, TAccountOwner extends string, TAccountAntAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: AdoptAuthorityAsyncInput<TAccountAsset, TAccountPayer, TAccountOwner, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): Promise<AdoptAuthorityInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountOwner, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>>;
export type AdoptAuthorityInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountOwner extends string = string, TAccountAntAuthority extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> = {
    /**
   * The Metaplex Core asset being adopted.
   * from raw data in the handler.
   */
    asset: Address<TAccountAsset>;
    /**
     * Pays for any rent the inner CPIs may charge. Often the same key as
     * `owner` but kept separate so a sponsor can pay.
     */
    payer: TransactionSigner<TAccountPayer>;
    /**
     * Must equal the asset's current MPL Core owner. On legacy ANTs Owner ==
     * UpdateAuthority == plugin authority, so this one signature authorizes both
     * the RevokePluginAuthorityV1 and the UpdateV1 UA rotation.
     */
    owner: TransactionSigner<TAccountOwner>;
    /** rotation. Address pinned by seeds; never read or written directly. */
    antAuthority: Address<TAccountAntAuthority>;
    /**
     * Metaplex Core program — pinned to the canonical ID; required for the
     * inner CPIs.
     */
    mplCoreProgram?: Address<TAccountMplCoreProgram>;
    systemProgram?: Address<TAccountSystemProgram>;
};
export declare function getAdoptAuthorityInstruction<TAccountAsset extends string, TAccountPayer extends string, TAccountOwner extends string, TAccountAntAuthority extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: AdoptAuthorityInput<TAccountAsset, TAccountPayer, TAccountOwner, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>, config?: {
    programAddress?: TProgramAddress;
}): AdoptAuthorityInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountOwner, TAccountAntAuthority, TAccountMplCoreProgram, TAccountSystemProgram>;
export type ParsedAdoptAuthorityInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * The Metaplex Core asset being adopted.
         * from raw data in the handler.
         */
        asset: TAccountMetas[0];
        /**
         * Pays for any rent the inner CPIs may charge. Often the same key as
         * `owner` but kept separate so a sponsor can pay.
         */
        payer: TAccountMetas[1];
        /**
         * Must equal the asset's current MPL Core owner. On legacy ANTs Owner ==
         * UpdateAuthority == plugin authority, so this one signature authorizes both
         * the RevokePluginAuthorityV1 and the UpdateV1 UA rotation.
         */
        owner: TAccountMetas[2];
        /** rotation. Address pinned by seeds; never read or written directly. */
        antAuthority: TAccountMetas[3];
        /**
         * Metaplex Core program — pinned to the canonical ID; required for the
         * inner CPIs.
         */
        mplCoreProgram: TAccountMetas[4];
        systemProgram: TAccountMetas[5];
    };
    data: AdoptAuthorityInstructionData;
};
export declare function parseAdoptAuthorityInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedAdoptAuthorityInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=adoptAuthority.d.ts.map