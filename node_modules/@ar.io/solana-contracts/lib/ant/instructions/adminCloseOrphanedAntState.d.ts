/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export declare const ADMIN_CLOSE_ORPHANED_ANT_STATE_DISCRIMINATOR: ReadonlyUint8Array;
export declare function getAdminCloseOrphanedAntStateDiscriminatorBytes(): ReadonlyUint8Array;
export type AdminCloseOrphanedAntStateInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountMigrationConfig extends string | AccountMeta<string> = string, TAccountConfig extends string | AccountMeta<string> = string, TAccountControllers extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> = Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? ReadonlyAccount<TAccountAsset> : TAccountAsset, TAccountMigrationConfig extends string ? ReadonlyAccount<TAccountMigrationConfig> : TAccountMigrationConfig, TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig, TAccountControllers extends string ? WritableAccount<TAccountControllers> : TAccountControllers, TAccountAuthority extends string ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, ...TRemainingAccounts]>;
export type AdminCloseOrphanedAntStateInstructionData = {
    discriminator: ReadonlyUint8Array;
};
export type AdminCloseOrphanedAntStateInstructionDataArgs = {};
export declare function getAdminCloseOrphanedAntStateInstructionDataEncoder(): FixedSizeEncoder<AdminCloseOrphanedAntStateInstructionDataArgs>;
export declare function getAdminCloseOrphanedAntStateInstructionDataDecoder(): FixedSizeDecoder<AdminCloseOrphanedAntStateInstructionData>;
export declare function getAdminCloseOrphanedAntStateInstructionDataCodec(): FixedSizeCodec<AdminCloseOrphanedAntStateInstructionDataArgs, AdminCloseOrphanedAntStateInstructionData>;
export type AdminCloseOrphanedAntStateAsyncInput<TAccountAsset extends string = string, TAccountMigrationConfig extends string = string, TAccountConfig extends string = string, TAccountControllers extends string = string, TAccountAuthority extends string = string> = {
    /**
   * Validated in the handler — refuses to proceed if the asset is
   * still mpl-core-owned (i.e. alive). PDA derivations below use
   * `asset.key()` directly so the asset and the closed PDAs are
   * cryptographically bound (security audit fix: previously a
   * separate `mint_key` field allowed an attacker to pass an
   * unrelated empty asset + a LIVE ANT's mint pubkey as `mint_key`,
   * closing the live ANT's state to themselves).
   */
    asset: Address<TAccountAsset>;
    /**
     * Admin authority gate. `has_one = authority` constrains the
     * signer to equal `AntMigrationConfig.authority` — without this,
     * the original ix was permissionless and would have let any
     * caller route per-ANT rent to themselves (security audit fix).
     */
    migrationConfig?: Address<TAccountMigrationConfig>;
    /**
     * AntConfig PDA — closed to `authority`. Seeded on the asset's
     * own pubkey (which survives mpl-core BurnV1 unchanged), so the
     * post-burn check on `asset` proves the live state below was
     * orphaned by THIS asset's burn — not some other live ANT.
     */
    config?: Address<TAccountConfig>;
    /**
     * AntControllers PDA — closed to `authority`. Same asset-bound
     * PDA derivation as `config`.
     */
    controllers?: Address<TAccountControllers>;
    /**
     * Protocol admin authority. Verified to equal
     * `migration_config.authority` via `has_one` above.
     */
    authority: TransactionSigner<TAccountAuthority>;
};
export declare function getAdminCloseOrphanedAntStateInstructionAsync<TAccountAsset extends string, TAccountMigrationConfig extends string, TAccountConfig extends string, TAccountControllers extends string, TAccountAuthority extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: AdminCloseOrphanedAntStateAsyncInput<TAccountAsset, TAccountMigrationConfig, TAccountConfig, TAccountControllers, TAccountAuthority>, config?: {
    programAddress?: TProgramAddress;
}): Promise<AdminCloseOrphanedAntStateInstruction<TProgramAddress, TAccountAsset, TAccountMigrationConfig, TAccountConfig, TAccountControllers, TAccountAuthority>>;
export type AdminCloseOrphanedAntStateInput<TAccountAsset extends string = string, TAccountMigrationConfig extends string = string, TAccountConfig extends string = string, TAccountControllers extends string = string, TAccountAuthority extends string = string> = {
    /**
   * Validated in the handler — refuses to proceed if the asset is
   * still mpl-core-owned (i.e. alive). PDA derivations below use
   * `asset.key()` directly so the asset and the closed PDAs are
   * cryptographically bound (security audit fix: previously a
   * separate `mint_key` field allowed an attacker to pass an
   * unrelated empty asset + a LIVE ANT's mint pubkey as `mint_key`,
   * closing the live ANT's state to themselves).
   */
    asset: Address<TAccountAsset>;
    /**
     * Admin authority gate. `has_one = authority` constrains the
     * signer to equal `AntMigrationConfig.authority` — without this,
     * the original ix was permissionless and would have let any
     * caller route per-ANT rent to themselves (security audit fix).
     */
    migrationConfig: Address<TAccountMigrationConfig>;
    /**
     * AntConfig PDA — closed to `authority`. Seeded on the asset's
     * own pubkey (which survives mpl-core BurnV1 unchanged), so the
     * post-burn check on `asset` proves the live state below was
     * orphaned by THIS asset's burn — not some other live ANT.
     */
    config: Address<TAccountConfig>;
    /**
     * AntControllers PDA — closed to `authority`. Same asset-bound
     * PDA derivation as `config`.
     */
    controllers: Address<TAccountControllers>;
    /**
     * Protocol admin authority. Verified to equal
     * `migration_config.authority` via `has_one` above.
     */
    authority: TransactionSigner<TAccountAuthority>;
};
export declare function getAdminCloseOrphanedAntStateInstruction<TAccountAsset extends string, TAccountMigrationConfig extends string, TAccountConfig extends string, TAccountControllers extends string, TAccountAuthority extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: AdminCloseOrphanedAntStateInput<TAccountAsset, TAccountMigrationConfig, TAccountConfig, TAccountControllers, TAccountAuthority>, config?: {
    programAddress?: TProgramAddress;
}): AdminCloseOrphanedAntStateInstruction<TProgramAddress, TAccountAsset, TAccountMigrationConfig, TAccountConfig, TAccountControllers, TAccountAuthority>;
export type ParsedAdminCloseOrphanedAntStateInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = {
    programAddress: Address<TProgram>;
    accounts: {
        /**
         * Validated in the handler — refuses to proceed if the asset is
         * still mpl-core-owned (i.e. alive). PDA derivations below use
         * `asset.key()` directly so the asset and the closed PDAs are
         * cryptographically bound (security audit fix: previously a
         * separate `mint_key` field allowed an attacker to pass an
         * unrelated empty asset + a LIVE ANT's mint pubkey as `mint_key`,
         * closing the live ANT's state to themselves).
         */
        asset: TAccountMetas[0];
        /**
         * Admin authority gate. `has_one = authority` constrains the
         * signer to equal `AntMigrationConfig.authority` — without this,
         * the original ix was permissionless and would have let any
         * caller route per-ANT rent to themselves (security audit fix).
         */
        migrationConfig: TAccountMetas[1];
        /**
         * AntConfig PDA — closed to `authority`. Seeded on the asset's
         * own pubkey (which survives mpl-core BurnV1 unchanged), so the
         * post-burn check on `asset` proves the live state below was
         * orphaned by THIS asset's burn — not some other live ANT.
         */
        config: TAccountMetas[2];
        /**
         * AntControllers PDA — closed to `authority`. Same asset-bound
         * PDA derivation as `config`.
         */
        controllers: TAccountMetas[3];
        /**
         * Protocol admin authority. Verified to equal
         * `migration_config.authority` via `has_one` above.
         */
        authority: TAccountMetas[4];
    };
    data: AdminCloseOrphanedAntStateInstructionData;
};
export declare function parseAdminCloseOrphanedAntStateInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedAdminCloseOrphanedAntStateInstruction<TProgram, TAccountMetas>;
//# sourceMappingURL=adminCloseOrphanedAntState.d.ts.map