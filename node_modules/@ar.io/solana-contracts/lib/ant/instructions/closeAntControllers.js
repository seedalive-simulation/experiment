/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findAntControllersPda } from '../pdas/index.js';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const CLOSE_ANT_CONTROLLERS_DISCRIMINATOR = new Uint8Array([96, 111, 116, 189, 119, 205, 204, 237]);
export function getCloseAntControllersDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ANT_CONTROLLERS_DISCRIMINATOR); }
export function getCloseAntControllersInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_ANT_CONTROLLERS_DISCRIMINATOR }));
}
export function getCloseAntControllersInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}
export function getCloseAntControllersInstructionDataCodec() {
    return combineCodec(getCloseAntControllersInstructionDataEncoder(), getCloseAntControllersInstructionDataDecoder());
}
export async function getCloseAntControllersInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: false }, controllers: { value: input.controllers ?? null, isWritable: true }, caller: { value: input.caller ?? null, isWritable: true } };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.controllers.value) {
        accounts.controllers.value = await findAntControllersPda({ asset: getAddressFromResolvedInstructionAccount("asset", accounts.asset.value) }, { programAddress });
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("controllers", accounts.controllers), getAccountMeta("caller", accounts.caller)], data: getCloseAntControllersInstructionDataEncoder().encode({}), programAddress });
}
export function getCloseAntControllersInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: false }, controllers: { value: input.controllers ?? null, isWritable: true }, caller: { value: input.caller ?? null, isWritable: true } };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("controllers", accounts.controllers), getAccountMeta("caller", accounts.caller)], data: getCloseAntControllersInstructionDataEncoder().encode({}), programAddress });
}
export function parseCloseAntControllersInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { asset: getNextAccount(), controllers: getNextAccount(), caller: getNextAccount() }, data: getCloseAntControllersInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=closeAntControllers.js.map