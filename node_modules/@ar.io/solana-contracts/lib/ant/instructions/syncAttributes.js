/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { addDecoderSizePrefix, addEncoderSizePrefix, combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getUtf8Decoder, getUtf8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount } from '@solana/program-client-core';
import { findAntAuthorityPda } from '../pdas/index.js';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../programs/index.js';
export const SYNC_ATTRIBUTES_DISCRIMINATOR = new Uint8Array([202, 89, 217, 75, 83, 91, 40, 34]);
export function getSyncAttributesDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(SYNC_ATTRIBUTES_DISCRIMINATOR); }
export function getSyncAttributesInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())]]), (value) => ({ ...value, discriminator: SYNC_ATTRIBUTES_DISCRIMINATOR }));
}
export function getSyncAttributesInstructionDataDecoder() {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())]]);
}
export function getSyncAttributesInstructionDataCodec() {
    return combineCodec(getSyncAttributesInstructionDataEncoder(), getSyncAttributesInstructionDataDecoder());
}
export async function getSyncAttributesInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, antAuthority: { value: input.antAuthority ?? null, isWritable: false }, arnsRecord: { value: input.arnsRecord ?? null, isWritable: false }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.antAuthority.value) {
        accounts.antAuthority.value = await findAntAuthorityPda({ asset: getAddressFromResolvedInstructionAccount("asset", accounts.asset.value) }, { programAddress });
    }
    if (!accounts.mplCoreProgram.value) {
        accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d';
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("payer", accounts.payer), getAccountMeta("antAuthority", accounts.antAuthority), getAccountMeta("arnsRecord", accounts.arnsRecord), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getSyncAttributesInstructionDataEncoder().encode(args), programAddress });
}
export function getSyncAttributesInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = { asset: { value: input.asset ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, antAuthority: { value: input.antAuthority ?? null, isWritable: false }, arnsRecord: { value: input.arnsRecord ?? null, isWritable: false }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, };
    // Resolve default values.
    if (!accounts.mplCoreProgram.value) {
        accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d';
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value = '11111111111111111111111111111111';
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("payer", accounts.payer), getAccountMeta("antAuthority", accounts.antAuthority), getAccountMeta("arnsRecord", accounts.arnsRecord), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getSyncAttributesInstructionDataEncoder().encode(args), programAddress });
}
export function parseSyncAttributesInstruction(instruction) {
    if (instruction.accounts.length < 6) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 6 });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return { programAddress: instruction.programAddress, accounts: { asset: getNextAccount(), payer: getNextAccount(), antAuthority: getNextAccount(), arnsRecord: getNextAccount(), mplCoreProgram: getNextAccount(), systemProgram: getNextAccount() }, data: getSyncAttributesInstructionDataDecoder().decode(instruction.data) };
}
//# sourceMappingURL=syncAttributes.js.map